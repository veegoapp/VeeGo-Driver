We are continuing the ActiveSession migration.

The shared infrastructure is already complete.
Ride migration is fully complete.
Shuttle Phase 1 is complete.

Current status:
- app/shuttle/trip-active already reads:
  - tripId
  - direction
from DriverShuttleSession.
Everything else still comes from ShuttleContext.

Use the attached ActiveSession contract as the ONLY source of truth.

THIS IS SHUTTLE PHASE 2.

Scope is intentionally limited.

You may modify ONLY:
- app/shuttle/trip-active.tsx

Do NOT modify:
- ShuttleContext
- ActiveSessionProvider
- ActiveSessionContext
- SocketProvider
- Ride files
- Shared hooks
- Navigation
- Backend
- Contract

Goal

Continue migrating display/read-only fields from ShuttleContext to DriverShuttleSession wherever the contract already contains an equivalent field.

Rules

1. Audit every remaining displayed value in app/shuttle/trip-active.tsx.
2. If DriverShuttleSession already contains an equivalent field, migrate it.
3. Keep DriverShuttleSession as the preferred source.
4. Keep temporary fallback to ShuttleContext only when absolutely necessary.
5. Do NOT invent mappings.
6. Do NOT reshape DriverShuttleSession.
7. Do NOT modify any mutation.
8. Do NOT modify any socket listener.
9. Do NOT modify ShuttleContext.
10. Do NOT remove old code.
11. Do NOT perform cleanup.
12. Do NOT change navigation.
13. Do NOT implement termination handling.
14. Do NOT change business logic.

Important

If a field cannot be migrated because:
- ShuttleContext derives additional state,
- the contract has a different structure,
- or migration would require changing business logic,

leave it unchanged and explicitly explain why.

After implementation:

Run:

tsc --noEmit

Return:

- Modified files.
- Every additional field migrated in this phase.
- Every remaining field still using ShuttleContext.
- Exact reason each remaining field could not be migrated.
- Confirmation that Ride files were untouched.
- Confirmation that Shared ActiveSession infrastructure was untouched.
- TypeScript result.