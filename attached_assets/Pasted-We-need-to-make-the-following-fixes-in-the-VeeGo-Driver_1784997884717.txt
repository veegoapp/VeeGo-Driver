We need to make the following fixes in the VeeGo Driver app.

IMPORTANT RULES:
- Only modify the items listed below.
- Do not refactor unrelated code.
- Do not change existing APIs or backend contracts.
- Do not add new features.
- Do not remove existing functionality except the explicitly mentioned removals.
- Apply changes one item at a time and keep the existing architecture and patterns.
- Do not modify screens, flows, or components that are unrelated to these changes.

Backend socket contracts are confirmed. Follow them exactly.

==================================================
1) Add FORCE_DISCONNECT handling
==================================================

Implement handling for socket event:

Event:
force:disconnect

Payload:
{
  reason: "Account blocked"
}

Required behavior:
- Listen for this event in the driver socket layer.
- Immediately disconnect the socket.
- Clear local authentication/session data.
- Prevent the driver from continuing to use the app.
- Navigate the driver to an account suspended/blocked screen.

Do not change the backend behavior.
Do not add new backend calls.

==================================================
2) Add DRIVER_HEARTBEAT emission
==================================================

Implement socket emission:

Event:
driver:heartbeat

Payload:
None.

Requirements:
- Emit every 30 seconds while the driver session is active and socket is connected.
- Continue during active trips.
- Do not emit faster than required.
- Clean up timers/listeners correctly to avoid duplicate intervals.

==================================================
3) Handle DRIVER SOS acknowledgement
==================================================

Implement socket listener:

Event:
driver:sos:ack

Possible payload:

{
  ok: true,
  message: "SOS received. Ops team has been alerted.",
  triggeredAt?: string
}

Required behavior:
- Show a clear confirmation to the driver that SOS was received.
- Do not implement admin acknowledgement logic.
- Do not listen for admin-only SOS events.

==================================================
4) Review shuttle driver socket trip events
==================================================

Ensure the driver app correctly emits these driver-specific shuttle events:

A) Start shuttle trip:

Event:
driver:trip:start

Payload:
{
  tripId: number
}

Emit when the driver starts a shuttle trip.

B) Complete shuttle trip:

Event:
driver:trip:complete

Payload:
{
  tripId: number
}

Emit when the driver completes a shuttle trip.

Do not use:
join:trip
leave:trip

Those events are passenger-only and should not be added for the driver app.

==================================================
5) Remove payment methods changed handling
==================================================

Do not implement:

payment:methods:changed

Remove any planned dependency on this event from this task.

The driver app should not react to admin payment method changes.

==================================================
6) Remove OTP requirement from this scope
==================================================

Do not add:
- OTP UI
- OTP verification
- OTP API calls
- Any ride-start OTP logic

The current backend does not support ride OTP.

Keep the existing ride start flow unchanged.

==================================================
7) Socket lifecycle constants cleanup (only if already simple)
==================================================

If there is an existing socket events constants pattern, move:

connect
disconnect
reconnect

to the appropriate constants location.

This is only a code organization change.

Do not refactor the socket architecture.

==================================================

After completing the changes:
- Provide a summary of files changed.
- Explain briefly what was changed in each file.
- Confirm that no other features were modified.