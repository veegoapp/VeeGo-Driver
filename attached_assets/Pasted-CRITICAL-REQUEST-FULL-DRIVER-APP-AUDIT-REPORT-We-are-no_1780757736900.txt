CRITICAL REQUEST — FULL DRIVER APP AUDIT REPORT

We are no longer debugging individual issues.

We need a COMPLETE SYSTEM AUDIT in a single Markdown report (.md).

----------------------------------------------------
GOAL
----------------------------------------------------
Explain EXACTLY how the Driver App behaves from:

- signup
- login
- service selection
- navigation
- main driver interface
- service restrictions (car / scooter / delivery / shuttle)

----------------------------------------------------
OUTPUT REQUIRED (MANDATORY)
----------------------------------------------------
Generate a full audit report in Markdown format including:

# DRIVER APP FULL SYSTEM AUDIT REPORT

----------------------------------------------------

1 — AUTH FLOW
- What happens on signup
- What happens on login
- What tokens are stored
- What storage keys are used

----------------------------------------------------

2 — ROUTING FLOW (CRITICAL)
- Exact file responsible for navigation after login
- Why user is routed to current screen
- Full decision tree

We want to know:
WHY user always lands on MAIN DRIVER INTERFACE immediately.

----------------------------------------------------

3 — SERVICE SELECTION FLOW
- When service-select is shown
- When it is skipped
- What triggers it

Explain why:
- New signup STILL bypasses service selection in some cases

----------------------------------------------------

4 — DRIVER MAIN INTERFACE ENTRY POINT
- Which file renders the map + online button screen
- Why it is shown immediately after login
- What condition allows access

----------------------------------------------------

5 — SERVICE CONTROL INTEGRATION
- How services (car / scooter / delivery / shuttle) are loaded
- Whether backend restrictions are respected or ignored
- Why disabled services are still accessible

----------------------------------------------------

6 — STORAGE ANALYSIS
List ALL keys and explain usage:

- veego_service_type
- auth_token
- refresh_token
- any onboarding flags

Explain:
- which ones are actually used for routing
- which ones are ignored

----------------------------------------------------

7 — EXPECTED vs ACTUAL FLOW

EXPECTED:
Signup → Service Select → Main Interface

ACTUAL:
Signup → Login → Main Interface مباشرة (skips selection)

Explain WHY.

----------------------------------------------------

8 — ROOT CAUSE SUMMARY
Give a clear bullet list of root causes:
- routing logic
- missing guards
- incorrect assumptions
- missing state checks

----------------------------------------------------

9 — DIAGRAM
Provide a simple flow diagram:

Signup/Login → ? → ? → Main Interface

----------------------------------------------------

IMPORTANT RULES
----------------------------------------------------
- NO fixes
- NO code changes
- NO feature suggestions
- ONLY analysis and evidence
- Must be based on actual code behavior, not assumptions

We need the truth of the system, not partial explanations.