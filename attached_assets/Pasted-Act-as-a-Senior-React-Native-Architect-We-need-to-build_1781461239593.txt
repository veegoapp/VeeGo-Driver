Act as a Senior React Native Architect. We need to build a brand new, dedicated Financial Analytics screen named `Earnings` inside the profile workflow (`app/shuttle/earnings.tsx`). 

Please note: Do NOT touch, modify, or look at any file named `history.tsx` or `wallet.tsx`. This is a completely independent cash-based earnings analytics ledger.

Please implement the following production architecture:

### 1. Minimalist White Financial Dashboard UI
- **Top Summary Cards (المركزي المالي):** Render a clean white card block divided into three distinct monetary sections using our RTL Arabic text:
  1. `"إجمالي الكاش المستلم"` (Total Cash Collected) -> Total physical cash the driver collected from passengers.
  2. `"عمولة التطبيق وعلينا"` (Platform Commission / Owed) -> The application's split-fee percentage (what the driver owes to the company).
  3. `"صافي أرباحك"` (Driver Net Profit) -> Calculated clean subtraction (Total Cash - Commission).
- **Timeframe Filters (الفلاتر):** Add a sleek horizontal layout selector at the top to filter these monetary analytics by: `"اليوم"` (Today), `"هذا الأسبوع"` (This Week), or `"هذا الشهر"` (This Month).
- **Financial Transaction Ledger:** A vertical list of detailed financial entries. Each item card must show: Date/Time, Operational Cash Received, and the exact App Commission deducted from that specific run.

### 2. Defensive State Engine & Localization
- Use `useI18n()` context to format all currency amounts cleanly with our dynamic EGP string configuration (`t.egp`).
- Implement standard pull-to-refresh (`RefreshControl`) to reload financial configurations.
- Apply full defensive state design—if the backend endpoints return errors or are missing, degrade gracefully into clear static placeholders with retry buttons without crashing.

### 3. API Contract Specifications & Explicit // TODOs
- Inside `lib/api.ts`, cleanly declare and document the explicit `// TODO: Backend Integration` endpoints powering this screen:
  - `GET /driver/financial-analytics?range=today|week|month` -> Returns `{ totalCash, appCommission, netProfit, transactions: [...] }`.
- Ensure strict TypeScript typing with zero compilation or variable leakage issues.

Proceed with building the entire `app/shuttle/earnings.tsx` screen from scratch!
