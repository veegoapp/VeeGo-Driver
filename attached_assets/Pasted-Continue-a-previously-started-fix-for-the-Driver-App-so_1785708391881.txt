Continue a previously started fix for the Driver App sound playback issue.

Important:
This is a continuation from another session. The previous AI session lost access because the token quota ended before finishing. Do not restart the investigation from scratch.

Current task:
Fix sound playback reliability for two sounds:

1) assets/sounds/trip_request.wav
Expected:
Play when a new on-demand ride request arrives:
- Car
- Scooter
- Delivery

Trigger flow:
Backend socket event:
"ride:offer"

Driver app flow:
hooks/useRideSocket.ts
→ ride:offer listener
→ app/(tabs)/home.tsx showRequest()
→ expo-av Audio.Sound.createAsync()

2) assets/sounds/shuttle-approaching.wav
Expected:
Play when a shuttle driver approaches a station.

Trigger flow:
hooks/useDriverLocation.ts
→ trip-active.tsx proximity calculation
→ phase becomes "approaching"
→ expo-av Audio.Sound.createAsync()

Investigation already completed in previous session:

Confirmed root cause:
The Sound object is created as a local variable inside async functions:

const { sound } = await Audio.Sound.createAsync(...)

The Sound instance is not stored in a component-level reference. This causes unreliable playback, especially with Expo New Architecture / EAS builds.

Required fix:
Use a component-level ref:

useRef<Audio.Sound | null>(null)

The Sound lifecycle should be:
- unload previous sound before creating a new one
- store the created Sound instance in the ref
- keep the existing playback trigger logic unchanged
- unload during component cleanup

Do NOT:
- change socket events
- change ride logic
- change shuttle proximity logic
- redesign audio system
- add unrelated fixes

Files that should be changed:

1) app/(tabs)/home.tsx

Previous session already started:
- sound ref was added
- playback block still needs to be verified and completed

Finish:
- connect the ref to trip_request.wav playback
- add cleanup if missing

2) app/shuttle/trip-active.tsx

Previous session intended:
- same sound ref lifecycle fix

Finish:
- connect the ref to shuttle-approaching.wav playback
- add cleanup if missing

3) app.json

Add:
"./assets/sounds/shuttle-approaching.wav"

to the existing notification sounds array only.

Before editing:
First inspect the current state of these files because some changes may already exist from the previous unfinished session.

After finishing:
Run TypeScript checks.

Return:
- Files changed
- What was already present vs what you completed
- Exact playback issue fixed
- Any remaining issues

Do not start a new audit. Continue from this partially completed implementation.