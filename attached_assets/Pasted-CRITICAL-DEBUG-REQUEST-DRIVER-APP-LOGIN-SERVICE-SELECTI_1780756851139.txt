CRITICAL DEBUG REQUEST — DRIVER APP LOGIN / SERVICE SELECTION FLOW

We need a full end-to-end explanation of what is happening in the Driver App after login.

Current issue:

- Driver logs in successfully using username + password
- After login, the app ALWAYS redirects to "Service Selection" screen
- This happens every time, not only on first signup
- Expected behavior: Service Selection should appear ONLY on first account creation (first-time onboarding), not on every login

----------------------------------------------------
GOAL
----------------------------------------------------
We need to understand the EXACT decision flow that determines where the user is routed after login.

----------------------------------------------------
REQUIRED ANALYSIS
----------------------------------------------------

1 — AUTH FLOW TRACE
Provide full step-by-step flow:

login request
→ token storage
→ session creation
→ routing decision
→ initial screen selection

2 — ROUTING LOGIC
Identify exactly where the app decides:

- Service Selection screen
- Main Driver Interface screen

We need:
- file names
- conditions used
- flags or storage keys checked

3 — ONBOARDING CONDITION CHECK
Explain what determines:

"first time user" vs "existing user"

We suspect there is a missing or incorrect condition causing onboarding to always trigger.

Check for:
- isFirstLogin
- isNewUser
- onboardingCompleted
- serviceSelected flag
- profile completion state

4 — BACKEND VS FRONTEND RESPONSIBILITY
Clarify:

- Is Service Selection controlled by backend response?
- Or purely frontend stored state?
- Or mixed logic?

5 — EXPECTED CORRECT FLOW

- First ever signup → show Service Selection
- After service selection is completed → mark onboarding as completed
- Every future login → go directly to Driver Home / Service Dashboard

6 — CURRENT BUG EXPLANATION

Explain exactly why the system is currently behaving like:

"always new user every login"

7 — DO NOT FIX YET

This is investigation only.
No code changes.
No refactors.
Only full explanation with evidence.

----------------------------------------------------
OUTPUT FORMAT
----------------------------------------------------
Provide:
- Flow diagram (step-by-step)
- Condition checks
- Storage keys used
- Root cause of repeated onboarding