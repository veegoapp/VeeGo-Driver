The attached ActiveSession contract is the ONLY source of truth.

Current phase:
Ride Interface Migration ONLY.

IMPORTANT:

Do NOT touch anything related to Shuttle.
Do NOT modify ShuttleContext.
Do NOT modify app/(shuttle).
Do NOT modify DriverShuttleSession logic.
Do NOT change any shuttle endpoint.
Do NOT remove any existing code yet.

This phase is additive only.

==================================================
Goal
==================================================

Replace the ride recovery entry point to use ActiveSession while keeping the existing ride implementation fully functional.

==================================================
Scope
==================================================

Only work on the Ride interface.

==================================================
Tasks
==================================================

1.

Update app/ride/[rideId].tsx.

Import:

useActiveSession()

2.

Read only:

- session
- initialized

Do NOT use:
- refreshActiveSession
- initializeActiveSession
- clearActiveSession

3.

If

initialized === true

AND

session?.sessionType === "ride"

then use that session as an additional source of truth.

Do NOT remove the existing GET /rides/:id query.

The query remains active.

4.

For fields that already exist in DriverRideSession, prefer the ActiveSession value with fallback to the old query.

Examples include:

- passenger.name
- passenger.phone
- pickup.address
- pickup.latitude
- pickup.longitude
- dropoff.address
- dropoff.latitude
- dropoff.longitude
- paymentMethod
- estimatedPrice
- finalPrice
- vehicleType

Fallback order:

ActiveSession
↓

existing ride query

5.

Do NOT migrate these fields yet.

Continue reading them ONLY from GET /rides/:id:

- rider.rating
- rider.avatar
- pickup.eta
- pickup.distance
- dropoff.distance
- duration
- driverId

6.

Do NOT remove:

GET /rides/:id

It remains the primary fallback.

7.

Do NOT change:

- handleNext()
- arrived/start/complete/cancel mutations
- waiting charge
- location tracking
- location broadcast
- ride socket handlers
- ownership check
- query invalidation
- STATUS_TO_PHASE
- completion flow

8.

Do NOT add any termination logic.

Ignore session == null for this phase.

Cancellation handling must remain exactly as it is today.

9.

Do NOT touch Home screen.

Phase A is already complete.

10.

Do NOT modify any Shuttle file.

==================================================
Verification
==================================================

After implementation report:

- every modified file
- every field now read from ActiveSession
- every field still read from GET /rides/:id
- confirm GET /rides/:id still exists
- confirm ride mutations are unchanged
- confirm socket handlers are unchanged
- confirm cancellation logic is unchanged
- confirm Shuttle was untouched
- run TypeScript
- report zero errors

Do not perform cleanup.
Do not remove old code.
Do not optimize anything outside this scope.