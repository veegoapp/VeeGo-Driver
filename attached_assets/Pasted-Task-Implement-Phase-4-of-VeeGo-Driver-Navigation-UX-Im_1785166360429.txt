Task: Implement Phase 4 of VeeGo Driver Navigation UX Improvements.

IMPORTANT CONTEXT:
This is after completing Navigation Phases 1, 2, and 3.

Current completed features:

Phase 1:
- Ride / Scooter / Delivery use useRoadPolyline.
- roadPolyline is passed to MapBackdrop.
- navigationMode works during:
  - to_pickup
  - in_trip
- Shuttle was not changed.

Phase 2:
- GPS updates improved.
- Camera animation improved.
- Bearing smoothing added.
- Driver marker movement animated.

Phase 3:
- New hooks/useNavigationRoute.ts added.
- Route progress trimming implemented.
- Off-route detection implemented.
- Rerouting implemented using existing /directions endpoint.
- Shuttle remains untouched.

DO NOT modify:
- Routing logic.
- useNavigationRoute.
- Trip state machine.
- Backend.
- APIs.
- Shuttle routing.

This phase is ONLY camera UX improvement.

==================================================

GOAL:

Make the navigation camera feel closer to Uber/Careem driver navigation.

Implement ONLY:

1. Look Ahead Camera
2. Auto Recenter after manual map movement

==================================================

PART 1 — Look Ahead Camera

Inspect:
components/MapBackdrop.native.tsx

Current navigation camera:
- animateCamera()
- pitch
- bearing
- zoom
- follows driver location.

Improve navigation camera positioning.

Requirements:

1. In navigationMode:
- Keep follow behavior.
- Keep bearing.
- Keep pitch.
- Keep zoom behavior.

2. Add a camera offset so the driver marker appears lower on the screen.

The goal:
Show more road area ahead of the driver.

Do not move the actual driver marker coordinate.
Only adjust the camera view.

3. The offset should work with:
- Ride
- Scooter
- Delivery
- Shuttle

4. Do not break:
- recenter button.
- focusTarget behavior in shuttle.

==================================================

PART 2 — Auto Recenter

Inspect existing manual pan logic.

Current behavior:
- onPanDrag sets userPanned state/ref.
- Follow camera stops.
- User manually presses recenter button.

Add automatic recovery.

Requirements:

1. When user manually pans:
- Keep current behavior.
- Mark camera as user controlled.

2. Start a timer.

After approximately 8 seconds:
- If navigationMode is still active:
  - Restore follow mode automatically.
  - Move camera back to driver location.
  - Restore bearing/pitch/zoom navigation view.

3. Cancel/reset timer if:
- User pans again.
- Navigation mode ends.
- Trip completes.

4. Do not remove the manual recenter button.
Keep both:
- Manual recenter.
- Automatic recenter.

==================================================

Safety Rules:

Do NOT:
- Add new dependencies.
- Add Google APIs.
- Change route calculations.
- Change GPS intervals.
- Change trip flow.
- Modify shuttle screen.

==================================================

Verification:

After implementation provide:

1. Files changed.
2. Exact changes.
3. How Look Ahead works.
4. How Auto Recenter works.
5. TypeScript/build result.
6. Confirmation:
   - Phase 1 unchanged.
   - Phase 2 unchanged.
   - Phase 3 unchanged.
   - Shuttle unaffected.

Do not implement any other navigation features.