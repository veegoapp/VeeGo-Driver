Implement the GPS architecture cleanup with the following approved plan and adjustments.

Goal:
Reduce duplicated GPS subscriptions by having one shared GPS source for the Driver App.

IMPORTANT:
Only make the changes described below.
Do not modify maps, camera, navigation, socket events, API contracts, UI, or background task behavior.

Before coding:
Review your planned file changes internally and keep the scope limited to the files listed.

Files allowed to change:

1. hooks/useGPSProvider.ts (new file)
Create a React context that owns the shared GPS subscription.

Requirements:
- It should be the single owner of watchPositionAsync.
- Expose:
  { position: DriverPosition | null, permissionDenied: boolean }
- Keep the provider mounted at root.
- IMPORTANT: Do NOT continuously consume GPS while the driver is offline.
- The provider should start watchPositionAsync only when location tracking is actually needed:
  - driver is online
  - active ride/trip tracking is enabled
- Stop the subscription when tracking is no longer needed.

2. hooks/useDriverLocation.ts
Keep the existing public API unchanged:

useDriverLocation(enabled?)

Do not change any callers.

Internally:
- Remove its own watchPositionAsync subscription.
- Read from GPSProvider context.
- Respect enabled flag by returning null when disabled.

3. hooks/useLocationBroadcast.ts
Remove its own getCurrentPositionAsync polling.

Replace the location source with GPSProvider shared position.

Keep all existing behavior:
- socket broadcasting
- REST fallback
- rideId/tripId branching
- background task suppression logic
- existing intervals/rate limits

IMPORTANT:
- Preserve the 5 second broadcast rate limit.
- Do not broadcast repeatedly if there is no meaningful location change.

4. hooks/useActiveLocationTracking.ts
Remove getCurrentPositionAsync usage.

Use the shared GPSProvider position.

Keep unchanged:
- 5 minute persistence interval
- offline queue
- batch sync
- reconnect flushing
- all existing API behavior

5. app/_layout.tsx
Mount GPSProvider once at the root provider tree.

6. hooks/index.ts
Export the new GPS hook if needed.

Files that must NOT change:
- components/MapBackdrop.native.tsx
- app/ride/[rideId].tsx
- app/shuttle/trip-active.tsx
- app/(tabs)/home.tsx
- app/(shuttle)/home.tsx
- lib/backgroundLocationTask.ts
- socket files
- API files
- UI files

Success criteria:
Before:
Multiple independent GPS consumers running simultaneously.

After:
One foreground GPS subscription shared by:
- driver marker
- socket/location broadcast
- REST tracking persistence

Background location task remains separate because it must work when the JS thread is suspended.

After implementation:
Provide:
1. List of changed files.
2. Summary of exactly what changed.
3. Confirmation that map, camera, navigation, sockets, APIs, and UI were untouched.