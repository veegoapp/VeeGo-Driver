You are working on the VeeGo Driver App (Expo SDK 54 / React Native / Expo Router). Below are 4 separate cleanup and polish tasks. Complete them in order. After finishing all 4, output a report titled "## What was done".

---

**TASK 1 — Move hardcoded strings to i18n**

Locations: app/(shuttle)/index.tsx, app/selfie.tsx, app/(shuttle)/wallet.tsx, app/service-select.tsx

What to fix — go through each file and move every hardcoded Arabic or English UI string into lib/i18nContext.tsx:

From app/(shuttle)/index.tsx:
- "التحقق مطلوب" and all related Arabic check-in banner text
- "Upcoming Trips", "No upcoming trips scheduled"
- "Renew your weekly slot", "remaining" (in countdown)

From app/selfie.tsx:
- All Arabic strings in shuttle check-in mode
- "Good lighting", "Face centered", "No glasses" tips

From app/(shuttle)/wallet.tsx:
- "Confirmed", "Pending", "Paid Out" status labels

From app/service-select.tsx:
- "Choose your service type", "Continue"

Steps:
1. Add new translation keys in both EN and AR objects inside lib/i18nContext.tsx
2. Replace each hardcoded string in the source files with the corresponding t.keyName from useI18n()
3. Keep all existing keys intact — only add new ones

---

**TASK 2 — Mark all notifications as read**

Location: app/messages.tsx

What to add:
- Add a "Mark all as read" button in the header/top area of the notifications screen
- Tapping it calls PATCH /notifications/read-all
- On success: refetch the notifications list so all items show as read visually
- Show a brief loading state on the button while the request is in progress
- If the request fails, show an error toast
- Only show the button if there is at least one unread notification in the list
- Use i18n for the button label

---

**TASK 3 — Dynamic unread notification badge**

Locations: app/(shuttle)/index.tsx and app/(tabs)/index.tsx (the bell icon area in both home screens)

What to fix:
- Currently the red dot on the bell icon is hardcoded and always visible
- On mount in both home screens, call GET /notifications and count items where isRead === false
- Store this count in a local state variable (unreadCount)
- Show the red dot only when unreadCount > 0
- If unreadCount > 0, show the number inside the dot (up to "9+" for counts above 9)
- When the driver navigates to the messages screen and comes back, refetch the count
- Also listen for the NOTIFICATION_NEW socket event (already connected from Phase 2 Task 4 in car home, and already in shuttleContext for shuttle home) — increment unreadCount by 1 when received

---

**TASK 4 — Remove dead code and fix Coming Soon alert**

What to remove/fix:

4a. Dead screens — do NOT delete the files (keep them for future use), but remove any navigation references pointing to them:
- app/onboarding.tsx: confirm there is no router.push('/onboarding') anywhere — if found, remove it
- app/language-select.tsx: confirm there is no router.push('/language-select') anywhere — if found, remove it

4b. Dead socket event constants — in constants/socketEvents.ts, add a JSDoc comment /* unused — reserved for future */ above these constants (do not delete them):
RIDE_NEW_REQUEST, DRIVER_TRIP_START, DRIVER_TRIP_COMPLETE, DRIVER_STATUS_ONLINE, DRIVER_STATUS_OFFLINE, DRIVER_STATUS_BUSY, DRIVER_LOCATION_ACK

4c. Coming Soon alert in wallet — in app/(shuttle)/wallet.tsx, find the History button that calls Alert.alert('Coming soon') and either:
- If a history list already exists in the screen → remove the Alert and wire the button to scroll to or show that section
- If no history exists → replace the Alert with a proper "Coming soon" styled banner component (not an Alert popup) that shows inline in the screen

4d. Unused api.ts endpoints — in lib/api.ts, add a JSDoc comment /* defined — not yet connected to UI */ above these endpoint definitions (do not delete them):
endpoints.trips.accept, endpoints.trips.reject, endpoints.trips.cancel, endpoints.shuttle.driverTrips, endpoints.wallet.addPayoutMethod (now connected after Phase 3), endpoints.wallet.removePayoutMethod (now connected after Phase 3)

---

After completing all 4 tasks output:
## What was done
- Task 1: [list every key added to i18nContext and every file modified]
- Task 2: [files modified, what changed]
- Task 3: [files modified, what changed]
- Task 4: [list 4a, 4b, 4c, 4d separately — what was found and what was done]