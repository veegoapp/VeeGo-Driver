We need a complete audit of the Driver App ActiveSession integration.

IMPORTANT:
- Driver App only.
- Do NOT modify any files.
- Do NOT refactor anything.
- This is an audit only.
- Return findings and recommendations only.

Context:
Backend ActiveSession is now complete:
- GET /api/driver/session exists.
- session:snapshot socket updates are emitted after ride and shuttle state transitions.
- DriverActiveSession is the source of truth for recovery.

Audit the Driver App for ActiveSession usage.

Check the following:

## 1) Shared ActiveSession Infrastructure

Audit:
- ActiveSessionProvider
- activeSessionContext
- initializeActiveSession()
- session:snapshot listener

Verify:
- Initial session loading works after login.
- Session updates replace old state correctly.
- session null handling is safe.
- No duplicate socket listeners.
- No memory leaks.

Report:
- file locations
- any risks

---

## 2) App Startup Recovery

Audit:

When the driver:
- kills the app during an active Ride
- kills the app during an active Shuttle trip
- opens the app again

Verify:

Ride:
- redirects to active ride screen.

Shuttle:
- redirects to active shuttle trip screen.

Check:
- navigation guards
- loading states
- possible redirect loops
- race conditions between ActiveSession and screen mounting

---

## 3) Ride Active Screen

Audit:
- app/ride/[rideId].tsx

Verify:

- Uses DriverRideSession correctly.
- No dependency on old GET /rides/:id for display.
- No stale old ride state usage.
- Mutations still work:
  - arrived
  - start
  - complete
  - cancel
  - SOS
  - rating

Check:
- socket listeners
- termination handling
- session null behavior

---

## 4) Shuttle Active Screen

Audit:
- app/shuttle/trip-active.tsx

Verify:

- ActiveSession coexistence with ShuttleContext.
- No conflicts between the two systems.
- Recovery works after app restart.
- session:snapshot updates do not break current logic.

Check:
- cold start behavior
- session termination handling
- navigation
- socket events
- current shuttle flow

---

## 5) Socket Audit

Verify:

- Which socket events the Driver App listens to.
- Whether ActiveSession and old contexts handle the same events.
- Any duplicate event handling.
- Any possible double navigation.

---

## 6) Final Classification

Return:

A) Confirmed working parts.

B) Bugs or risks found.

C) Severity:
- Critical
- High
- Medium
- Low

D) Whether the Driver App is ready for Pilot testing.

Do not modify files.
Run TypeScript check only if needed for verification.