Task: Perform a complete audit of the Driver App map camera and navigation behavior for ALL ride services (Ride, Scooter, Delivery, Shuttle, and any future services).

IMPORTANT:
- Do NOT modify any code.
- Do NOT create commits.
- Do NOT implement fixes.
- Only inspect the codebase and provide a detailed technical report.

Background:
The current behavior appears to display the route in an overview/bird's-eye mode after a trip is accepted. The map shows the entire route from above instead of behaving like Google Maps navigation.

Expected behavior:
When a driver starts navigating to a pickup point or destination, the map should behave similarly to Google Maps navigation:
- Camera follows the driver's current location.
- Camera rotates based on the driver's heading/bearing.
- Camera uses a pitched (angled) navigation view instead of a top-down view.
- Camera continuously updates while the driver moves.
- Camera automatically adjusts when approaching turns if supported.
- The experience should be consistent across every supported service.

Please inspect the entire Driver App and answer the following:

1. Which files are responsible for:
   - Map rendering
   - Camera control
   - Route drawing
   - Driver location updates
   - Driver heading/bearing updates
   - Navigation state management

2. For each supported service (Ride, Scooter, Delivery, Shuttle):
   - Which screen is used?
   - Which map component is used?
   - Is the implementation shared or duplicated?
   - Does each service have different camera logic?

3. After a trip is accepted:
   - What sequence of functions is executed?
   - Which function moves the camera?
   - Which function draws the route?
   - Which function updates driver location?

4. Search the entire project for usage of:
   - fitToCoordinates()
   - fitToSuppliedMarkers()
   - animateToRegion()
   - animateCamera()
   - setCamera()
   - followUserLocation
   - bearing
   - heading
   - pitch
   - zoom

5. Determine:
   - Is the camera always forced into overview mode?
   - Does the app ever enter a navigation/follow mode?
   - Is there any existing follow-camera implementation that is disabled or incomplete?

6. Verify whether driver heading is available:
   - Is heading received from GPS?
   - Is heading ignored?
   - Is heading smoothed?
   - Is heading passed into the map camera?

7. Check if there is logic for:
   - Auto-follow
   - Auto-recenter
   - User manually moving the map
   - Re-enabling follow mode
   - Navigation camera state

8. Review how routes are rendered:
   - Google Directions API
   - Encoded polylines
   - react-native-maps Polyline
   - Any third-party navigation library

9. Identify every limitation preventing Google Maps-style navigation.

10. Recommend the cleanest architecture for implementing a reusable navigation camera system that works across all ride services without duplicating logic.

Finally, provide:
- A list of affected files.
- A list of root causes.
- A prioritized implementation plan.
- Any risks or edge cases.
- Do NOT write code or modify files.