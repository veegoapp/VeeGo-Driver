We are continuing an ActiveSession migration project.

IMPORTANT CONTEXT

- The shared ActiveSession infrastructure is already complete.
- Ride interface migration is fully complete.
- Shuttle home cold-start recovery is already complete.
- We are now working ONLY on the Shuttle interface.
- Do NOT modify any Ride files.
- Do NOT modify the shared ActiveSession infrastructure.
- Use the attached ActiveSession contract as the only source of truth.
- Do not invent fields that do not exist in the contract.

THIS IS PHASE 1 OF THE SHUTTLE MIGRATION.

Scope is intentionally limited.

You may modify ONLY:
- app/shuttle/trip-active
- Any tiny supporting helper only if absolutely required for TypeScript.

Do NOT modify:
- app/(shuttle)/home
- Any Ride file
- ActiveSessionProvider
- ActiveSessionContext
- SocketProvider
- Shared hooks
- Shared navigation
- Backend
- Contract

Goal of this phase

Migrate the Shuttle Active Trip screen to read display/read-only data from DriverShuttleSession instead of ShuttleContext or the old active-trip data.

Rules

1. Audit the screen against the attached ActiveSession contract before changing anything.
2. Migrate ONLY display/read-only fields that already exist in DriverShuttleSession.
3. Keep DriverShuttleSession as the preferred source.
4. Keep temporary fallback ONLY where the contract does not provide the required field.
5. Do NOT invent missing fields.
6. Keep all existing mutations unchanged.
7. Keep all existing socket listeners unchanged.
8. Keep polling/refresh logic if it is still required for non-migrated features.
9. Do NOT remove ShuttleContext in this phase.
10. Do NOT change navigation.
11. Do NOT implement termination logic.
12. Do NOT remove old code.
13. Do NOT perform cleanup.
14. Do NOT refactor unrelated code.
15. Do NOT make architecture changes outside this scope.

Before implementing:

- Audit every displayed field used by app/shuttle/trip-active.
- Compare each field with DriverShuttleSession in the attached contract.
- Identify:
  - which fields can be migrated immediately,
  - which fields cannot be migrated because the contract does not contain them.
- Then implement ONLY the fields that are safely supported by the contract.

After implementation:

Run:

tsc --noEmit

Return:

- Modified files.
- Every migrated field.
- Every field still using the old source and the exact reason it could not be migrated.
- Confirmation that Ride files were untouched.
- Confirmation that shared ActiveSession infrastructure was untouched.
- TypeScript result.