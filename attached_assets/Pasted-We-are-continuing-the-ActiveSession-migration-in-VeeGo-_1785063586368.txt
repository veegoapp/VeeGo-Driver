We are continuing the ActiveSession migration in VeeGo Driver App.

Important architecture rule:
There are TWO separate interfaces:
1. Shuttle interface: /(shuttle)
2. Other services interface (Car/Scooter/Delivery): /(tabs)

Do NOT mix shuttle logic with ride/service logic.

Current migration plan:
1. Build shared ActiveSession foundation for both interfaces.
2. Finish migration of non-shuttle services.
3. Finish shuttle migration.
4. Remove old systems and cleanup.

Already completed:
- ActiveSessionProvider exists at root level and wraps both interfaces.
- ActiveSessionInitBridge was added in app/_layout.tsx.
- initializeActiveSession() now runs after authentication.
- session:snapshot socket flow remains unchanged.
- /(tabs)/home.tsx already redirects active rides using ActiveSession.
- app/ride/[rideId].tsx has started reading ride data from ActiveSession while old GET /rides/:id still exists temporarily.
- No old code has been deleted yet.

Current task:
Implement ONLY ride termination handling in app/ride/[rideId].tsx.

Goal:
When ActiveSession receives:
session:snapshot { data: null }

the ride screen should react and exit the ride screen using the existing exit flow.

Requirements:
- Use useActiveSession().
- Detect when the active session becomes null after initialization.
- Call the existing exitRide() function or existing ride exit mechanism already used by this screen.
- Keep existing behavior intact.

Do NOT:
- Do not touch shuttle files.
- Do not migrate shuttle logic.
- Do not remove GET /rides/:id yet.
- Do not change ride mutations.
- Do not change socket ride event handlers.
- Do not change cancellation logic.
- Do not add new navigation architecture.
- Do not redesign the screen.

This is a small migration step only:
ActiveSession termination event should become an additional source of truth for leaving the active ride screen.

After implementation:
1. Run TypeScript check.
2. Report exactly:
- files modified
- what changed
- confirmation that shuttle code was untouched
- confirmation that existing ride socket/mutation logic was untouched
- TypeScript result