Task: Implement Phase 3 of the VeeGo Driver App Navigation Improvements.

IMPORTANT:
This is a continuation of previous navigation work, but this chat session may not have previous history.

Current completed state:

PHASE 1 COMPLETED:
- Ride / Scooter / Delivery use app/ride/[rideId].tsx.
- Ride screen now uses useRoadPolyline.
- roadPolyline is passed to MapBackdrop.
- navigationMode is enabled during:
  - to_pickup
  - in_trip
- Shuttle behavior was preserved.

PHASE 2 COMPLETED:
- hooks/useDriverLocation.ts:
  - timeInterval improved from 3000ms to 1000ms.
  - distanceInterval improved from 10m to 4m.

- components/MapBackdrop.native.tsx:
  - Camera animation improved.
  - Bearing smoothing added using EMA.
  - Driver marker movement is animated smoothly.

Current architecture:
- MapBackdrop.native.tsx:
  - Responsible for map rendering.
  - Responsible for camera behavior.
  - Responsible for polyline rendering.
  - Receives route through roadPolyline prop.

- useRoadPolyline.ts:
  - Existing reusable route fetching hook.
  - Uses existing backend directions endpoint.

Do NOT undo Phase 1 or Phase 2.

==================================================

PHASE 3 GOAL:

Add navigation intelligence:
1. Detect when the driver leaves the route.
2. Recalculate the route from the driver's current location.
3. Show only the remaining route progress instead of the already completed route.

The target experience is similar to Uber/Careem driver navigation.

==================================================

PART 1 — Route Progress / Remaining Route

First inspect the current implementation.

Goal:
The displayed route should represent the remaining journey.

Example:

Current:
Driver has completed half the route:

🚗-----------------------------📍

Expected:
The completed part disappears:

🚗-------------📍


Requirements:

1. Do not change backend route generation.
2. Do not create a new routing service.
3. Use the existing roadPolyline coordinates.
4. Calculate the driver's closest position on the route.
5. Render only the remaining polyline points after the driver's current position.

Must support:
- Ride:
  - to_pickup route
  - in_trip route

- Scooter:
  - Same flow as Ride.

- Delivery:
  - Same flow as Ride.

- Shuttle:
  - Preserve current behavior.
  - Do not break station markers or segment routing.

==================================================

PART 2 — Off Route Detection

Add detection for when the driver leaves the current route.

Requirements:

1. Use the existing driverLocation updates.

2. Calculate distance between:
- Driver current location
- Nearest point on current route polyline

3. Define a reasonable threshold.

Example:
- If driver is more than approximately 50 meters away from route:
  - Consider the driver off-route.

Do not trigger rerouting from tiny GPS errors.

==================================================

PART 3 — Rerouting

When the driver is confirmed off-route:

Requirements:

1. Request a new route using the existing routing system.

2. New route should start from:
- Current driver location

And end at:
- Current active destination.

Destination rules:

Ride / Scooter / Delivery:

to_pickup:
Driver → Pickup

in_trip:
Driver → Dropoff


Shuttle:
Do not redesign shuttle routing.
Keep existing station-to-station routing behavior.

3. Avoid excessive API calls.

Implement protection against:
- Multiple reroute requests at the same time.
- Repeated rerouting while GPS is unstable.

Consider:
- Cooldown timer.
- Minimum movement before another reroute.

==================================================

PART 4 — Architecture Requirements

Before coding, inspect whether this logic belongs in:

Option A:
Existing screen files.

Option B:
A reusable navigation hook/service.

Prefer a reusable solution if possible.

Possible direction:
hooks/useNavigationRoute.ts

Responsible for:
- Current route.
- Remaining route.
- Off-route detection.
- Reroute trigger.

Do not create unnecessary abstractions.

==================================================

PART 5 — Safety Requirements

Do NOT modify:

- Authentication.
- Trip state machine.
- Booking logic.
- Payment logic.
- Driver status logic.
- Backend APIs unless absolutely required.

Do NOT add:
- New paid APIs.
- Google Navigation SDK.
- External dependencies.

==================================================

Verification Required:

After implementation provide:

1. Files changed.
2. Exact changes made.
3. How rerouting works.
4. How route progress works.
5. Threshold values used.
6. TypeScript/build verification.
7. Confirmation:

- Ride works.
- Scooter works.
- Delivery works.
- Shuttle was not broken.
- Phase 1 navigation behavior remains intact.
- Phase 2 camera improvements remain intact.

Do not continue to additional navigation features.