You are performing a FULL DRIVER APP STABILIZATION PHASE.

DO NOT IMPLEMENT ANY NEW FEATURES.

DO NOT ADD PLACEHOLDERS.

DO NOT ADD MOCK DATA.

ONLY FIX EXISTING ARCHITECTURAL, STATE, NAVIGATION, SOCKET, AUTH, AND DATA INTEGRITY ISSUES IDENTIFIED IN THE AUDIT.

Before modifying anything:

1. Scan the entire project.
2. Verify every finding against actual source code.
3. Fix only issues that are real and reproducible.
4. After implementation generate:

PROJECT_UPDATE_REPORT.md

The report must contain:

* Every file changed
* Every bug fixed
* Every bug intentionally not fixed
* Any blockers
* Any backend requirements
* Any remaining mock data
* Any remaining dead endpoints
* Final system health summary

========================================
PHASE A — REMOVE SERVICE SWITCH SYSTEM
======================================

The service switch mechanism must be completely removed.

Requirements:

* Remove switch-service buttons from all profile screens.
* Remove any menu items related to switching services.
* Remove navigation paths that exist solely for switching service type.
* Remove unused service switching helpers.
* Ensure service selection happens ONLY:

  * First login
  * Explicit logout + new account
  * Manual reset (if such feature already exists)

After removal:

* No screen inside the app should expose service switching.
* Drivers must stay in their selected service until logout or reset.

========================================
PHASE B — TOKEN REFRESH RACE FIX
================================

Audit finding:

Multiple concurrent 401 responses can trigger multiple refresh requests.

Implement:

Single-flight refresh pattern.

Requirements:

* Only one refresh request may exist at a time.
* Other failed requests must await the same promise.
* Prevent refresh storms.
* Prevent accidental logout caused by parallel refreshes.

Add debug logs:

[AUTH_REFRESH_START]
[AUTH_REFRESH_REUSED]
[AUTH_REFRESH_SUCCESS]
[AUTH_REFRESH_FAILED]

========================================
PHASE C — UNIFY SOCKET ARCHITECTURE
===================================

Audit finding:

Two independent socket.io connections currently exist.

Requirements:

Create a single shared socket lifecycle.

Target:

* ServiceControl
* Ride events
* Surge events
* Notifications
* Future realtime modules

All consumers must subscribe to the shared socket.

After completion:

Only ONE active socket connection per authenticated session.

Add logs:

[SOCKET_CONNECT]
[SOCKET_DISCONNECT]
[SOCKET_RECONNECT]
[SOCKET_SHARED_INSTANCE]

========================================
PHASE D — SHUTTLE HOME STATUS
=============================

Audit finding:

online defaults to true.

Requirements:

Remove:

const [online] = useState(true)

Fetch actual status from backend.

If endpoint exists:

GET /driver/me/status

Use it.

If another endpoint already contains online state:

Use that instead.

UI must always reflect real server state.

Never assume online=true.

========================================
PHASE E — SHUTTLE PROFILE DATA
==============================

Audit finding:

Hardcoded profile stats.

Remove all fake values:

97%
12 routes
3 new
1 unread

Requirements:

Use backend data if available.

If backend data unavailable:

Hide the stat entirely.

Never show fake values.

========================================
PHASE F — SHUTTLE WALLET
========================

Audit finding:

Hardcoded bank account.

Remove:

BIAT ****4521

Requirements:

Use:

GET /driver/wallet/payout-methods

Display real payout methods.

If none exist:

Show clean empty state.

Never display hardcoded banking information.

========================================
PHASE G — SAFETY SCREEN
=======================

Audit finding:

Safety items route to support.

Requirements:

Do not pretend features exist.

For each non-implemented feature:

Show:

Coming Soon

or

Unavailable

Do NOT redirect to support.

Make behavior honest and explicit.

========================================
PHASE H — HARDCODED DATA CLEANUP
================================

Remove or replace:

* Lac 2 area is busy
* Hardcoded unread counters
* Hardcoded notification counts
* Hardcoded route counts
* Hardcoded on-time percentages
* Hardcoded bank info

App version:

Read from Expo config if available.

========================================
PHASE I — DEAD ENDPOINT AUDIT
=============================

Inspect every endpoint listed in audit.

Classify each:

ACTIVE
PARTIAL
DEAD

Do NOT delete endpoints automatically.

Generate a section in PROJECT_UPDATE_REPORT.md:

Dead Endpoints Review

Include:

Endpoint
Status
Referenced Files
Recommendation

========================================
PHASE J — SERVICE SELECT & ROUTING VALIDATION
=============================================

Validate:

* Login flow
* Signup flow
* Logout flow
* Cold start
* Returning driver
* New driver on previously-used device
* Shuttle driver
* Car driver

Verify:

service-select appears ONLY when expected.

Verify:

No remaining invalid:

router.replace('/service-select')

calls.

Verify:

No navigation warnings remain.

========================================
FINAL REPORT
============

Generate:

PROJECT_UPDATE_REPORT.md

Include:

1. Executive Summary

2. Files Modified

3. Architecture Changes

4. Auth Changes

5. Socket Changes

6. Shuttle Changes

7. Wallet Changes

8. Safety Changes

9. Remaining Mock Data

10. Remaining Dead Endpoints

11. Remaining Technical Debt

12. Production Readiness Score

13. Pass / Fail result for:

* Authentication
* Navigation
* Service Selection
* Service Control
* Shuttle Flow
* Wallet
* Notifications
* Realtime Socket Layer

Do not stop after typecheck.

Actually inspect the codebase and verify every fix before generating the report.
