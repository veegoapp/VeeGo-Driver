You are working on the VeeGo Driver App (Expo SDK 54 / React Native / Expo Router). Backend base URL is EXPO_PUBLIC_API_URL, auth via Bearer token. Below are 3 separate tasks. Complete them in order. After finishing all 3, output a report titled "## What was done".

---

**TASK 1 — Fix currency: replace all "DT" with EGP symbol**

Locations: app/(shuttle)/wallet.tsx and anywhere else in the codebase that hardcodes "DT"

What to fix:
- Search the entire codebase for the string "DT" used as a currency symbol
- Replace every occurrence with "ج.م" (the existing i18n key is t.egp — use that key everywhere instead of hardcoding)
- Also check that the earnings summary cards, transaction history rows, payout screen, and wallet balance all use t.egp from i18nContext
- Do not change any variable names or logic — only the displayed currency string

---

**TASK 2 — Add payout method add/remove UI**

Location: app/(shuttle)/wallet.tsx (payout methods section)

What to build:
- Currently the screen shows the payout methods list and a payout button but has no way to add or remove a method
- Add an "Add payout method" button below the methods list
- Tapping it opens a modal or bottom sheet with a form:
  - Type selector (options: bank_transfer, vodafone_cash, instapay — show as radio buttons or segmented control)
  - Account number field (numeric keyboard)
  - Account name field (text)
  - Bank name field (shown only when type is bank_transfer)
  - Phone number field (shown only when type is vodafone_cash or instapay)
  - Submit button → calls POST /driver/wallet/payout-methods with { type, accountNumber, accountName, bankName?, phoneNumber? }
  - Show loading on submit, show error if it fails, close modal and refetch list on success
- Each existing payout method row must have a delete button (trash icon)
  - Tapping it shows a confirmation alert
  - On confirm → calls DELETE /driver/wallet/payout-methods/:id
  - On success → refetch the payout methods list
- Use i18n for all new strings
- Support RTL layout

---

**TASK 3 — Build the Bonus Targets screen**

Create a new screen: app/bonus-targets.tsx

What to build:
- Full screen showing the driver's active bonus targets and their progress
- On mount, call GET /driver/bonus-targets
- The response contains an array of bonus targets, each with:
  { id, title, description, targetType, targetValue, bonusAmount, progress, vehicleType, startsAt, endsAt, isActive }
- For each target, show a card with:
  - Title and description
  - A progress bar showing progress / targetValue (0–100%)
  - The bonus amount they will earn: "Bonus: X ج.م"
  - Valid until date (endsAt formatted as DD/MM/YYYY)
  - A status badge: "In progress" (green) if not yet reached, "Completed" (teal) if progress >= targetValue
- If the list is empty, show an empty state: "No active bonus targets right now"
- Show a loading skeleton while fetching
- Show an error state with retry button if fetch fails
- Use i18n for all strings
- Link to this screen from app/(shuttle)/profile.tsx and app/(tabs)/profile.tsx — add a "Bonus targets" list item in the profile menu

---

After completing all 3 tasks output:
## What was done
- Task 1: [files modified, what changed]
- Task 2: [files modified, what changed]
- Task 3: [files created/modified, what changed]