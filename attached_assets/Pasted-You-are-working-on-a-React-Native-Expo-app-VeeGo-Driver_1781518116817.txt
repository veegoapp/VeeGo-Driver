You are working on a React Native Expo app (VeeGo Driver - Shuttle module).

We already have the full backend state machine defined and stable.
Your job is STRICTLY frontend refactor to match backend 1:1.

────────────────────────────
🚨 CORE PRINCIPLE (NON-NEGOTIABLE)
────────────────────────────
The backend is the ONLY source of truth.

Frontend must NEVER:
- guess timing
- use weekday logic
- infer renewal windows
- use cron assumptions
- decide state transitions

ONLY use:
booking.status from backend.

Valid statuses:
- active
- pending_renewal
- cancelled
- expired

────────────────────────────
🎯 REQUIRED REFACTOR
────────────────────────────

### 1. REMOVE ALL TIME-BASED LOGIC
Delete completely:
- any getDay() checks
- any UTC day filtering
- any cron window assumptions
- any "renewal window open/close" logic
- any logic based on renewalDeadline for decisions

renewalDeadline is DISPLAY ONLY.

────────────────────────────

### 2. UI STATE RULES (STRICT)

#### Banner
Show ONLY if:
booking.status === "pending_renewal"

#### Confirm Renewal Button
Enabled ONLY if:
booking.status === "pending_renewal"

Otherwise:
- disabled
- no API call allowed

#### Cancel / Decline Button
MUST ALWAYS WORK
- remove any global guards or shared boolean locks
- ensure no state can block click handlers

────────────────────────────

### 3. COUNTDOWN HANDLING

- renewalDeadline is ONLY for display
- NEVER used for logic
- use safe parsing:
  const t = Date.parse(renewalDeadline)

Rules:
- if invalid → show "--"
- if null → show "--"
- if status !== pending_renewal → hide countdown completely (optional safe UI)

Do NOT use it to determine UI state.

────────────────────────────

### 4. API BEHAVIOR FIX

File: lib/api.ts

Fix confirm renewal flow:
- Before calling API:
  if booking.status !== "pending_renewal"
    → show alert: "Renewal not available"
    → DO NOT call backend

- On error:
  ALWAYS display backend response:
  err.response?.data?.error

Never show generic error unless backend provides nothing.

────────────────────────────

### 5. STATE SYNC RULE

After ANY of these actions:
- confirm renewal
- decline renewal
- cancel booking

You MUST:
- refetch booking from backend
- update UI state from server response

NO local optimistic state only.

────────────────────────────

### 6. REMOVE THESE ENTIRELY

Search and delete:
- renewalAlertOpen global lock issues
- any "stuck modal state" logic
- any weekday-based banner conditions
- any cron-related UI assumptions

────────────────────────────

### 7. FILES TO MODIFY ONLY

Return full updated versions of:
- lib/shuttleContext.tsx
- app/(shuttle)/bookings.tsx
- lib/api.ts (only if needed)

DO NOT change backend.
DO NOT redesign UI.
DO NOT add features.
ONLY fix logic to match backend state machine.

────────────────────────────

🎯 GOAL
Frontend must become a pure reflection of backend state machine with zero independent business logic.