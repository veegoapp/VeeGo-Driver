Act as a Senior Software Architect. Excellent job delivering the Push Notification support and providing this precise technical breakdown of the active trip cycle. 

Let's proceed immediately to close all three remaining gaps (Gap A, Gap B, and Gap C) in one parallel execution to finalize the complete Shuttle Trip Cycle.

Please implement the following step-by-step:

### 1. Close Gap A: Trip Start API Binding
- In `trip-details.tsx`, inside the "بدء الرحلة" (Start Trip) `onPress`, replace the empty TODO with a clear, async simulation calling the backend endpoint: `endpoints.shuttle.start(bookingId)`.
- Ensure it captures the returned `tripId` from the backend and saves it globally in the `shuttleContext` or state so it can be referenced throughout the active route.

### 2. Close Gap B: Build `shuttle/trip-complete.tsx` (Earnings & Wallet Update)
- Create the new screen: `app/shuttle/trip-complete.tsx` using a clean, minimalist white theme layout.
- This screen must be triggered right after `handleFinishRoute()` receives a successful response from `endpoints.shuttle.complete(activeLine.id)`.
- **Data Presentation:** It must receive and display the dynamic backend response payload: `{ earnedAmount, walletBalance }`. 
- **UI Elements:** - Show the earnings summary clearly in local currency ("جنيه" / "EGP").
  - Add a success confirmation card with a subtle animation or clean layout.
  - Add a Call-To-Action (CTA) button: "العودة للرئيسية" (Return to Home) that clears the active trip states from the context and safely navigates back to `app/(shuttle)/index.tsx`.

### 3. Close Gap C: Map Routing Polyline for Fixed Routes
- Update `MapBackdrop` inside `trip-active.tsx` to accept a new prop: `routePolyline` (an array of ordered station coordinates).
- Upon mounting `trip-active.tsx`, pass the ordered stations (`data.stations[]` coordinates) to `MapBackdrop`.
- Inside `MapBackdrop`, simulate fetching or rendering the polyline overlay connecting these fixed stations sequentially, so the driver sees the full predetermined route drawn on the map, not just their live position.

### Execution Instructions:
- Maintain zero TypeScript errors and perfect typings across all context updates and new screen params.
- Strictly adhere to the modern, clean, white-layout visual theme.
- Ensure all backend API points, state updates, and local currency strings are heavily documented with clear `// TODO: Backend Integration` comments where applicable.

Let's deliver these three fixes simultaneously. Proceed with the implementation!
