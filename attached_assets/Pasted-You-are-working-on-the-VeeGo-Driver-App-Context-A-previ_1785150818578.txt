You are working on the VeeGo Driver App.

Context:
A previous currency audit was already completed on the Driver App.
The goal is that the Driver App should be Egypt-only and use EGP as the only currency.

Previous audit findings were fixed already.

The following fixes were implemented:

1) app/(tabs)/history.tsx
- Replaced hardcoded "DT" currency display with the i18n currency key t.egp.

2) app/trips/[tripId].tsx
- Replaced hardcoded "DT" currency display with t.egp.

3) app/shuttle/trip-active.tsx
- Replaced the USD-looking cash emoji 💵 with a neutral Banknote icon.

4) app/shuttle/trip-complete.tsx
- Replaced hardcoded "EGP" strings with t.egp for:
  - cash collected
  - cash shortfall warnings
  - card totals
  - wallet totals
  - trip total

5) app/shuttle/trip-cancel.tsx
- Replaced hardcoded EGP penalty text with translation-based messages.

6) lib/serviceControlContext.tsx
- Added validation so backend currency objects are accepted only when code === "EGP".
- Non-EGP values fall back to the default EGP configuration.

7) app/(tabs)/earnings.tsx
- Replaced DollarSign icon with a neutral icon.

8) app/bonus-targets.tsx
- Changed number formatting locale from en-US to en-EG.

9) Date formatting fixes:
- app/(shuttle)/wallet.tsx
- app/(tabs)/wallet.tsx
- app/(tabs)/home.tsx
- app/support.tsx

Now perform a final verification pass only.

Important:
Do not modify any files.
Do not refactor.
Do not add new fixes.
Only audit and report findings.

==================================================
1) Full Currency Regression Scan
==================================================

Scan the entire Driver App codebase:

- app/
- components/
- lib/

Search for any remaining currency-related issues:

- DT
- TND
- USD
- $
- DollarSign
- dollar-sign
- €
- SAR
- AED
- EUR
- any foreign currency strings
- any hardcoded currency labels
- any hardcoded "EGP" that should use i18n

Verify:

- All monetary displays use t.egp or the correct formatter.
- No screen displays a non-EGP currency.
- No UI icon visually represents another currency.
- Earnings, wallet, history, trip details, shuttle screens, and cancellation flows are consistent.
- Number formatting uses Egypt-compatible locale where appropriate.


==================================================
2) Translation Keys Verification
==================================================

Verify the translation keys used in the previous fixes:

- t.egp
- t.cancel_penalty_preview

Check:

- They exist in the translation type/interface.
- They exist in English translations.
- They exist in Arabic translations.
- Arabic values are correct.
- No runtime fallback or missing key risk exists.


==================================================
Final Report Format
==================================================

Return only a verification report.

Include:

1. Checks that passed ✅
2. Remaining issues ❌ (if any)
3. Exact file locations for any remaining problems
4. Severity of remaining issues

Do not make any code changes.