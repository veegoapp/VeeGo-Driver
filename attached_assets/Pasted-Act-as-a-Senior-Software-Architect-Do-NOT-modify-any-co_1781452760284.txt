Act as a Senior Software Architect. Do NOT modify any code, do NOT implement any fixes, and do NOT create mock APIs yet. We need to perform a strict feasibility and contract audit for a set of proposed hardening enhancements to the active trip lifecycle.

Please analyze the current project files (specifically `app/shuttle/trip-active.tsx`, `app/(shuttle)/index.tsx`, `lib/api.ts`, and any existing global state hooks) and generate a `HARDENING_FEASIBILITY_REPORT.md` addressing the following proposals:

1. **Active Trip Exit Guard:** Can we safely intercept navigation or back-press using our current version of Expo Router/React Navigation without breaking modal or sheet layouts? What specific mechanism is supported on disk?
2. **Explicit No-Show Passenger Reporting:** Is there any hidden or alternative endpoint/payload already defined in the codebase for a "No-Show" event, or does the backend currently only infer it from the absence of a boarding confirmation?
3. **Boarding Failure Visibility & Retry Flow:** If a boarding API request fails, what is the safest way to expose the error to the driver without breaking the current layout, and what files would be impacted?
4. **Separate Loading States:** Can we isolate the arrival and completion loading states safely without altering the reactive core context state machine?
5. **Timeout Auto-Advance Lifecycle Synchronization:** What assumptions are we making about the `SHUTTLE_STATION_TIMEOUT` socket event? Does the backend intend to handle station completion implicitly on its side, or is the client expected to fire a patch request first?
6. **Typed Shuttle Completion Response Contract:** What is the actual envelope shape currently returned by `endpoints.shuttle.complete` in our types or shared declarations?
7. **Dynamic Currency Configuration:** Does the app already possess a localization or tenant config module where the currency symbol can be drawn, or is it purely static across the entire codebase?

For each proposal, explicitly determine:
- Is the required backend structure/endpoint already implemented?
- Is the required payload contract defined?
- Is the proposed change safe, or could it break existing operational flows?
- What exact assumptions are being made?

Deliver a comprehensive, objective report so we can plan our next steps securely.
