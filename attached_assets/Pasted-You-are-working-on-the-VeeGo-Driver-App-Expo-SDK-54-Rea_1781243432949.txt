You are working on the VeeGo Driver App (Expo SDK 54 / React Native / Expo Router). Backend base URL is EXPO_PUBLIC_API_URL, auth via Bearer token. Below are 4 separate tasks for the shuttle service. Complete them in order. After finishing all 4, output a report titled "## What was done".

---

**TASK 1 — No-show button in the boarding screen**

Location: app/shuttle/boarding.tsx

What to build:
- For each passenger row in the boarding list, there must be two action buttons:
  1. ✓ Boarded → already exists (calls POST /shuttle/bookings/:bookingId/board)
  2. ✗ No-show → NEW: calls PATCH /driver/bookings/:bookingId/no-show
- The no-show button must show a confirmation alert ("Are you sure this passenger did not show up?") before making the API call
- After a successful no-show call, mark that passenger row visually as absent (different color/icon, disabled buttons)
- If the API call fails, show an error toast/alert and keep the row in its previous state
- Both buttons must be disabled after either action is taken for that passenger (no double-tapping)
- Use i18n for all new strings

---

**TASK 2 — Wire station arrived and station completed buttons**

Location: app/shuttle/trip-active.tsx

What to fix:
- The functions stationArrived() and stationCompleted() exist in lib/api.ts but are not called from any UI button
- Add two buttons that appear at the correct moment in the trip flow:
  1. "Arrived at station" button → calls PATCH /driver/trips/:tripId/stations/:stationId/arrived — show this button when the driver is navigating TO a station (before arrival)
  2. "Station completed" button → calls PATCH /driver/trips/:tripId/stations/:stationId/completed — show this button after arrival is confirmed, when the driver is ready to depart to the next station
- The current stationId must come from the active stop being tracked (already in shuttleContext or local state)
- Disable each button after it is tapped and while the API call is in progress
- Show an error toast if either call fails, without blocking the trip flow
- Use i18n for all new strings

---

**TASK 3 — Handle missing shuttle socket events**

Locations: lib/shuttleContext.tsx and app/(shuttle)/index.tsx

What to add — 3 socket event handlers that are defined in constants/socketEvents.ts but have no handler anywhere:

3a. SHUTTLE_STATION_TIMEOUT (shuttle:station:timeout)
- Listen for this event in app/shuttle/trip-active.tsx or shuttleContext
- When received, show an alert/banner to the driver: "Time at this station has expired — please proceed to the next station"
- Auto-advance to the next station in the local stop tracking state
- Use i18n for the message

3b. SHUTTLE_RENEWAL_CONFIRMED (shuttle:renewal:confirmed)
- Listen in lib/shuttleContext.tsx
- When received, show a success toast/notification: "Your booking for next week has been confirmed"
- Invalidate/refetch the shuttle-my-bookings query so the UI updates
- Use i18n

3c. SHUTTLE_BOOKING_CREATED (shuttle:booking:created)
- Listen in lib/shuttleContext.tsx
- When received, invalidate/refetch the shuttle-my-bookings query so the new booking appears immediately
- No visible notification needed for this one — silent refresh is enough

---

**TASK 4 — Handle notification:new in car home screen**

Location: app/(tabs)/index.tsx

What to fix:
- Currently notification:new socket event is only handled in shuttleContext.tsx
- The car home screen has no listener for this event
- Add a listener for NOTIFICATION_NEW in app/(tabs)/index.tsx
- When received, show a brief toast notification at the top of the screen with the notification title and body from the event payload
- Also increment a local unread count state variable (used in Task 3 of Phase 4 for the badge)

---

After completing all 4 tasks output:
## What was done
- Task 1: [files modified, what changed]
- Task 2: [files modified, what changed]
- Task 3: [files modified, what changed — 3a, 3b, 3c separately]
- Task 4: [files modified, what changed]