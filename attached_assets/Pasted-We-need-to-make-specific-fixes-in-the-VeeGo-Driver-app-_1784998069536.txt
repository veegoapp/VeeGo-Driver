We need to make specific fixes in the VeeGo Driver app.

IMPORTANT RULES:
- Modify ONLY the items listed below.
- Do not refactor unrelated code.
- Do not change backend APIs or socket contracts.
- Do not add new features.
- Do not change existing user flows except where explicitly requested.
- Do not modify shuttle logic while working on on-demand services, and do not modify on-demand logic while working on shuttle services.
- Keep the existing architecture, folder structure, and coding patterns.
- Do not fix any issue that is not listed in this request.

Backend socket contracts are confirmed. Follow them exactly.

==================================================
PART 1 — APP-WIDE SOCKET / SESSION FIXES
==================================================

These changes apply to the whole driver application.

--------------------------------------------------
1) Handle FORCE_DISCONNECT
--------------------------------------------------

Socket event:

force:disconnect

Payload:

{
  reason: "Account blocked"
}

Implement:

- Add a listener in the driver socket layer.
- When received:
  1. Disconnect the socket immediately.
  2. Clear local authentication/session data.
  3. Prevent the driver from continuing to use the app.
  4. Navigate to an account suspended/blocked screen.

Do not:
- Add backend calls.
- Change authentication logic outside this flow.
- Modify normal logout behavior.


--------------------------------------------------
2) Add DRIVER_HEARTBEAT
--------------------------------------------------

Socket event:

driver:heartbeat

Payload:
None.

Implement:

- Emit this event every 30 seconds while:
  - Driver session is active.
  - Socket connection exists.

Requirements:
- Continue during active rides.
- Continue regardless of online/busy state.
- Prevent duplicate intervals.
- Clean up timers correctly.

Do not change existing location tracking.


--------------------------------------------------
3) Handle DRIVER SOS acknowledgement
--------------------------------------------------

Socket event:

driver:sos:ack

Payload:

{
  ok: true,
  message: string,
  triggeredAt?: string
}

Implement:

- Listen for this event.
- Show a clear confirmation message to the driver that SOS was received.

Do not:
- Handle admin-only SOS acknowledgement events.
- Change SOS sending logic.


==================================================
PART 2 — ON-DEMAND SERVICES
(Car / Scooter / Delivery)
==================================================

Only modify on-demand ride flow.

--------------------------------------------------
4) Remove payment methods socket dependency
--------------------------------------------------

Do not implement:

payment:methods:changed

Remove only any dependency related to this event from the current task.

The driver app should not update payment methods through this socket event.

Do not modify:
- Payment APIs.
- Fare calculation.
- Ride payment flow.


--------------------------------------------------
5) Keep current ride start flow unchanged
--------------------------------------------------

Do not add any OTP functionality.

Do not add:
- OTP screens.
- OTP inputs.
- OTP verification.
- OTP APIs.
- Ride-start OTP logic.

The backend does not support ride OTP.

Important:
There is no existing ride OTP feature to remove.
Only keep the current Start Ride behavior unchanged.


==================================================
PART 3 — SHUTTLE SERVICE
==================================================

Only modify shuttle-related socket trip lifecycle events.

--------------------------------------------------
6) Add driver shuttle trip socket events
--------------------------------------------------

The driver app must emit:

A) Start shuttle trip

Event:

driver:trip:start

Payload:

{
  tripId: number
}

Emit when the driver starts a shuttle trip.


B) Complete shuttle trip

Event:

driver:trip:complete

Payload:

{
  tripId: number
}

Emit when the driver completes a shuttle trip.


Do not use:

join:trip
leave:trip

These are passenger-only events and must not be added for the driver app.


==================================================
PART 4 — CODE ORGANIZATION ONLY
==================================================

--------------------------------------------------
7) Socket lifecycle constants
--------------------------------------------------

Only if the driver app already has a socket events constants pattern:

Move:

connect
disconnect
reconnect

into the appropriate constants location.

This is only a code organization change.

Do not refactor socket architecture.


==================================================
FINAL RESPONSE REQUIRED
==================================================

After completing the changes, provide:

1. List of modified files.
2. Short explanation of each file change.
3. Confirmation that no unrelated features were modified.