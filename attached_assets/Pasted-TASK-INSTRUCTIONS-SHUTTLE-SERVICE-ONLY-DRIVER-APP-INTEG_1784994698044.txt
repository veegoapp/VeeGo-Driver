TASK INSTRUCTIONS: SHUTTLE SERVICE ONLY — DRIVER APP INTEGRATION WITH BACKEND ARCHITECTURE.
​Strict Scope: Apply 4 specific Shuttle integration fixes ONLY based on the verified backend audit report. Do NOT touch any other services (Car, Scooter, Delivery) or any unrelated files. Maintain clean code with zero TypeScript errors. Execute strictly step-by-step.
​Step 1: Mid-Trip Station State Recovery (lib/shuttle/ShuttleContext.tsx)
​Listen for the backend socket event "shuttle:state:sync". On receipt of { action: "refetch" }, immediately re-fetch GET /driver/trips/:id/stations.
​Do NOT rely on an in-memory or single currentStopIndex variable. Determine the current station dynamically from the GET /driver/trips/:id/stations response array:
​Station with status === "arrived" -> Current station the driver is at.
​First station with status === "pending" (sorted by order) -> Next station to drive towards.
​Stations with status === "completed" -> Finished stops.
​Step 2: Automatic Passenger Cancellation Handler
Context: Passenger cancellations are triggered automatically when a passenger cancels via their app (or via admin). The driver must be notified in real-time without admin intervention.
​Listen for the socket event "shuttle:booking:cancelled" on the driver socket room.
​Expected Payload: { bookingId: number, routeId: number, reason: string | null }.
​Action:
​Find and immediately remove the matching bookingId from the active station's manifest in local state.
​Trigger GET /driver/trips/:id/stations in the background to ensure complete state synchronization.
​Step 3: Correct SOS Endpoint & Payload (app/shuttle/trip-active.tsx)
​Update the SOS API endpoint to strictly use: POST /driver/shuttle/trips/:tripId/sos
​Expected JSON Body:
{ "latitude": number, "longitude": number, "notes": string }
(Note: latitude and longitude are required finite numbers. notes is optional up to 500 chars).
​Gracefully handle response HTTP 201 ({ sosId, message }) and error codes (404, 400, 500).
​Step 4: Dynamic Station ETAs (app/shuttle/ screens)
​Stop calculating ETAs locally using mock or hardcoded strings.
​Fetch dynamic ETAs from the dedicated endpoint: GET /driver/trips/:id/stations/eta
​Call this endpoint on screen mount and after recording station transitions ("arrived" / "completed").
​Read etaMinutes from nextStation and remainingStations to display real arrival estimates to the driver.
​EXECUTION RULES:
​Work through Step 1 to Step 4 sequentially.
​Strictly adhere to standard Shuttle types (lib/shuttle/types.ts).
​Verify zero TypeScript errors (tsc --noEmit) upon completion.
​Do NOT alter UI layouts, design styling, or non-shuttle logic.