Implement the Driver App currency cleanup fixes based on the completed currency audit.

IMPORTANT:
- Make code changes only for the listed currency/display issues.
- Do NOT modify backend APIs.
- Do NOT modify payment logic.
- Do NOT modify wallet calculations.
- Do NOT modify earnings calculations.
- Do NOT change business logic.
- Preserve all existing functionality.

Goal:
VeeGo operates in Egypt only.
All money displays must be EGP-only and localization-aware.

Required fixes:

1) Remove Tunisian Dinar (DT) from ride history

File:
app/(tabs)/history.tsx

Current:
Fare display uses:
`${fare.toFixed(2)} DT`

Fix:
Replace the hardcoded DT with the existing translation currency key:
t('egp')

Expected:
English:
45.00 EGP

Arabic:
45.00 ج.م

Do not change the fare value source.

---

2) Remove Tunisian Dinar (DT) from trip details

File:
app/trips/[tripId].tsx

Current:
`${fare.toFixed(2)} DT`

Fix:
Replace DT with t('egp').

Expected:
Use the same localization behavior as the rest of the app.

---

3) Replace US dollar cash emoji

File:
app/shuttle/trip-active.tsx

Current:
Cash payment indicator uses:
💵

Problem:
The emoji visually represents a US dollar bill and conflicts with Egypt-only branding.

Fix:
Replace with a neutral cash/payment icon.

Allowed:
- Wallet icon
- Banknote icon
- Coins icon

Do not use:
- Dollar sign
- Currency-specific icons

Keep the text/payment logic unchanged.

---

4) Replace hardcoded EGP strings with translation

File:
app/shuttle/trip-complete.tsx

Replace all hardcoded:
"EGP"

with:
t('egp')

Affected locations:
- line 166
- line 170
- line 190
- line 210
- line 222

Reason:
Arabic users should see:
ج.م

not:
EGP

---

5) Replace hardcoded EGP strings in cancellation

File:
app/shuttle/trip-cancel.tsx

Replace:
"EGP"

with:
t('egp')

Locations:
- line 112
- line 186

---

6) Add EGP safety guard for backend currency object

File:
lib/serviceControlContext.tsx

Current:
extractCurrency() trusts backend currency.symbol.

Fix:
Add validation:

Only accept backend currency when:
code === "EGP"

If backend returns:
USD
TND
EUR
SAR
AED
or any unknown value

fallback to:
EGP / ج.م

Do not allow foreign currency symbols into the app UI.

---

7) Remove dollar icon from earnings screen

File:
app/(tabs)/earnings.tsx

Current:
Lucide DollarSign icon used for confirmed earnings.

Problem:
It visually suggests USD.

Fix:
Replace with a neutral financial icon.

Examples:
- CheckCircle
- TrendingUp
- Wallet

Do not change the amount display.

---

8) Fix bonus amount locale

File:
app/bonus-targets.tsx

Current:
toLocaleString('en-US')

Fix:
Use Egypt locale:
'en-EG'

or follow existing app money formatting utility if available.

Do not change calculations.

---

9) Fix date locale consistency

Files:
app/(shuttle)/wallet.tsx
app/(tabs)/wallet.tsx
app/(tabs)/home.tsx
app/support.tsx

Current:
toLocaleDateString() without locale.

Fix:
Use explicit locale:
'en-EG'
or
'ar-EG'
depending on existing i18n language handling.

Do not modify date logic.

---

After changes provide:

1. Files modified.
2. Exact changes made.
3. Confirmation:
- No backend changes.
- No API changes.
- No wallet logic changes.
- No earnings calculation changes.
- No payment logic changes.
- All Driver App currency displays are now EGP-only.
4. Run TypeScript validation if available.