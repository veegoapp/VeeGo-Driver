Act as a Senior React Native Architect. We need to completely rewrite and refactor the Driver's Bookings Screen (`app/(shuttle)/bookings.tsx`) and its corresponding Detail Sheet to serve as the production control center for our weekly commitments.

Please implement the following architecture:

### 1. Unified Layout & Week Filtering
- **Layout Theme:** Clean, minimalist white design.
- **Week Tabs:** Add secondary filter tabs at the top: "الأسبوع الحالي" (Current Week) and "الأسبوع القادم" (Next Week) to easily segment the 5-day blocks (Sunday to Thursday).
- **Upcoming Section:** Lists the grouped weekly route blocks. Tapping any active block opens the advanced `BookingDetailSheet`.
- **Completed/Past Section:** Accessible via a tab or clear breakdown that lists past finished trips with their final stats: route name, total passengers, total revenue, and net driver earnings cleanly formatted with the local currency string ("جنيه" / "EGP").

### 2. Wednesday Renewal Banner (Priority Retention UI)
- If the context indicates `renewalPromptActive` is true (simulating Wednesday after 7:00 AM Cairo Time), render a high-priority retention banner at the top of the bookings list.
- **Content:** Display the Arabic text asking the driver to renew their slot commitment for the upcoming week. Include a dynamic 10-hour countdown timer (Deadline: Wednesday 5:00 PM Cairo Time).
- **Actions:** 
  - "تأكيد التجديد" (Confirm) -> fires `endpoints.shuttle.renewBooking(bookingId)`.
  - "اعتذار عن الخط" (Decline) -> fires `endpoints.shuttle.declineRenewal(bookingId)` to instantly release the slot.

### 3. Advanced BookingDetailSheet (The Control Center)
Tapping an upcoming week block must open a bottom sheet that renders real-time data driven by the backend:
- **Dynamic Threshold Status Badge:** Read the server state. If the passenger count hasn't met the minimum requirement, badge it as "بانتظار اكتمال الحد الأدنى للركاب" (Pending Status). If met, badge it as "نشط - كمل الحد الأدنى" (Active Status).
- **Live Passenger Counter:** A clean visual progress tracker showing booked seats vs total bus capacity (e.g., 7 / 14 passengers). Ensure a socket listener or focus effect handles real-time updates for this count.
- **Core Driver Actions:**
  - **"إلغاء الرحلة" (Cancel Trip):** Tapping this opens our dynamic cancellation flow/modal (`app/shuttle/direct-cancel.tsx`) passing the required context.
  - **"تحويل الرحلة لسائق آخر" (Refer Trip):** Tapping this opens the referral workflow (`app/shuttle/referral-request.tsx`) to input a peer driver's code.

### 4. Backend Integration Framework (// TODOs)
Since the backend coverage for these specific contracts is undergoing active verification, carefully document `lib/api.ts` and the component layers with exact `// TODO: Backend Integration` comments tracking:
- `GET /shuttle/driver/bookings` parsing both backend-translated route/station strings and segregated temporal data.
- Live passenger count synchronization channels via Socket.io.
- Renewal response mapping payloads.

Ensure strict TypeScript validation with zero compilation errors. Proceed with the complete rewrite.
