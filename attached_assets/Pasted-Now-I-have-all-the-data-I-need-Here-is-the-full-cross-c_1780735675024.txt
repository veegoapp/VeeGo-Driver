Now I have all the data I need. Here is the full cross-check verdict for every finding in the audit against the actual backend source code (not the contract doc, which is frequently stale).

Cross-Check Verdict Report
Legend: CONTRACT GAP = the contract doc was wrong, real backend differs; FRONTEND BUG = backend is fine, app is wrong; BACKEND MISSING = endpoint genuinely does not exist; SHARED = both sides contribute to the problem.

Authentication
Audit Finding	Verdict	Reason
OTP path POST /auth/send-otp "not in contract"	CONTRACT GAP	POST /api/auth/send-otp exists in routes/auth.ts. The contract doc omitted it.
OTP verify POST /auth/verify-otp "not in contract"	CONTRACT GAP	POST /api/auth/verify-otp exists in routes/auth.ts.
Driver login path mismatch (/driver/auth/login vs /auth/login)	CONTRACT GAP	POST /api/driver/auth/login exists in routes/driver.ts. App path is correct; contract doc was wrong.
Driver register path mismatch (/driver/auth/register vs /auth/driver/register)	SHARED	Path POST /api/driver/auth/register exists and matches the app. However, the app's register payload omits required vehicle fields (vehicleType, plateNumber, make, model, year, color) — that part is a real frontend bug.
Logout path mismatch (/driver/auth/logout vs /auth/logout)	CONTRACT GAP	POST /api/driver/auth/logout exists in routes/driver.ts. App path is correct.
Driver Profile & Status
Audit Finding	Verdict	Reason
GET /driver/me flagged as wrong (should be /driver/profile)	CONTRACT GAP	Backend has GET /api/driver/me in routes/driver.ts. App is correct; contract doc invented the /profile path.
PATCH /driver/me same	CONTRACT GAP	Same — backend has PATCH /api/driver/me.
Go online/offline split paths (should be single /driver/status)	CONTRACT GAP	Backend has separate PATCH /api/driver/status/online and PATCH /api/driver/status/offline — exactly what the app calls. Contract doc incorrectly said single endpoint.
GET /driver/me/status flagged (should be /driver/status)	CONTRACT GAP	Backend has GET /api/driver/me/status. App is correct.
GET /driver/me/vehicle flagged as "no self-service GET"	CONTRACT GAP	Backend has GET /api/driver/me/vehicle in routes/driver.ts. It exists.
GET /driver/me/ratings flagged (should be /driver/reviews)	CONTRACT GAP	Backend has GET /api/driver/me/ratings. App is correct.
Ride Lifecycle
Audit Finding	Verdict	Reason
Polling GET /driver/rides/available "not in contract"	CONTRACT GAP	Backend has GET /api/driver/rides/available in routes/rides.ts. Endpoint exists. Whether polling is the right architecture vs. socket push is a design question but it is not broken.
start called with POST instead of PATCH	FRONTEND BUG	Backend only defines PATCH /api/driver/rides/:id/start. No confirmed POST alias for start found in the source — only the deprecated POST for decline was confirmed. App will get 404/405.
complete called with POST instead of PATCH	FRONTEND BUG	Same — backend defines PATCH only.
decline called with POST instead of PATCH	CONTRACT GAP	Backend has router.post("/driver/rides/:id/decline") explicitly (commented as deprecated TODO). App's POST will hit this handler and work, despite the contract saying PATCH.
Missing GET /api/driver/rides/active hydration on launch	FRONTEND BUG	Endpoint exists in backend; app simply never calls it.
Missing PATCH /api/driver/rides/:id/cancel for driver cancel	FRONTEND BUG	PATCH /api/rides/:id/cancel exists in backend (routes/rides.ts). UI just has no button for it.
Driver Trips (Shuttle)
Audit Finding	Verdict	Reason
All 10 trip endpoints marked ✅ correct	Confirmed correct	Verified against routes/driver.ts. No issues.
Board booking: POST /shuttle/stops/:id/board vs contract PATCH /api/driver/bookings/:id/board	FRONTEND BUG	Backend has POST /api/shuttle/bookings/:id/board in routes/shuttle.ts — method (POST) matches, but the path prefix is bookings, not stops. The app sends a batch { boardedIds[] } but the backend operates on a single booking ID in the path. App path and payload are both wrong.
Missing PATCH /api/driver/bookings/:id/absent	FRONTEND BUG	If it's in the contract, the backend likely has it (in routes/bookings.ts or routes/shuttle.ts). No UI exists for it in the app.
Shuttle Lines
Audit Finding	Verdict	Reason
POST /shuttle/lines/:id/activate "not in contract"	BACKEND MISSING	Confirmed absent from all route files. The correct pattern is PATCH /api/driver/trips/:id/start.
POST /shuttle/lines/:id/complete "not in contract"	BACKEND MISSING	Same — no such route exists. Use trips endpoint.
GET /shuttle/lines/:id/passengers "not in contract"	FRONTEND BUG	Backend has GET /api/shuttle/trips/:id/passengers (trips, not lines). Wrong entity in the path.
POST /shuttle/lines/:id/book "not in contract"	BACKEND MISSING	No booking-by-line endpoint found in any route file.
Earnings
Audit Finding	Verdict	Reason
Summary + weekly paths both ✅ correct	Confirmed correct	routes/earnings.ts has both.
todayEarnings mock still rendered, no backend endpoint	SHARED	App renders mock data (frontend bug) and backend has no endpoint serving that exact breakdown (backend missing). Both sides need work.
weekEarnings falls back to mock on failure	FRONTEND BUG	API is wired; the fallback to mock data on error is a resilience choice but renders false data. Backend endpoint is fine.
Wallet
Audit Finding	Verdict	Reason
GET /wallet wrong (should be /driver/wallet/balance)	FRONTEND BUG	Backend has GET /api/driver/wallet/balance in routes/driver.ts. App calls the passenger wallet path.
GET /wallet/transactions for driver	FRONTEND BUG	GET /api/wallet/transactions exists in routes/wallet.ts but is the passenger wallet endpoint. Driver should use earnings history. App is hitting the wrong endpoint.
Payout methods hardcoded (BIAT, Visa)	FRONTEND BUG	GET /api/driver/wallet/payout-methods exists in backend; app ignores the response and renders hardcoded cards.
Documents & Check-In
Audit Finding	Verdict	Reason
GET /driver/me/documents flagged (should be /driver-documents/by-driver/:driverId)	CONTRACT GAP	Backend has GET /api/driver/me/documents directly in routes/driver.ts. App path is correct; contract doc pointed to the admin route by mistake.
POST /driver/me/documents flagged (should be /driver-documents/upload/:driverId)	CONTRACT GAP	Backend has POST /api/driver/me/documents in routes/driver.ts. App path is correct.
Documents screen renders lib/mock.ts:documents, no live fetch	FRONTEND BUG	Endpoint exists and path is correct; the screen just never calls it.
Selfie check-in path (POST /api/checkin)	CONTRACT GAP	Backend has POST /api/driver/checkin (not /api/checkin). App and contract doc both need updating to the real path.
Check-in status not called	FRONTEND BUG	GET /api/driver/checkin/status exists in routes/checkin.ts. App never calls it.
Notifications & Settings
Audit Finding	Verdict	Reason
GET /notifications "passenger-only"	CONTRACT GAP	Backend handler uses only authenticate (no requireRole), meaning it serves all authenticated users including drivers. App path is correct.
PATCH /notifications/:id/read "no driver endpoint"	CONTRACT GAP	Backend has PATCH /api/notifications/:id/read in routes/notifications.ts with no role restriction. Works for drivers.
GET /driver/me/settings flagged (should be /driver/settings)	CONTRACT GAP	Backend has both GET /api/driver/me/settings (original) and GET /api/driver/settings (alias added later, marked // FIXED). App path works against the original.
PATCH /driver/me/settings same	CONTRACT GAP	Same — both aliases exist in routes/driver.ts.
Push Token Registration
Audit Finding	Verdict	Reason
POST /users/me/push-token "not in contract"	CONTRACT GAP	Backend has POST /api/users/me/push-token in routes/users.ts with a proper Zod-validated handler. App path is correct; contract doc missed it.
Socket.IO
Audit Finding	Verdict	Reason
App listens to "ride:offer" but contract says server emits "ride:new_request"	CONTRACT GAP	dispatch-manager.ts emits SOCKET_EVENTS.RIDE_OFFER = "ride:offer". App is listening to the correct event. Contract doc was wrong.
ride:offer_expired — no listener	FRONTEND BUG	Backend emits "ride:offer_expired" (RIDE_OFFER_EXPIRED). App has no handler; offer overlay never auto-dismisses.
ride:waiting:charge:updated — no listener	FRONTEND BUG	Backend emits "ride:waiting:charge:updated" (WAITING_CHARGE_UPDATED). Audit used wrong event name (ride:waiting_charge_updated with underscores); real name has colons. Frontend still missing the listener either way.
ride:waiting:charge:capped — no listener	FRONTEND BUG	Same pattern — backend emits it, app has no handler.
ride:free_window_ended — no listener	CONTRACT GAP	Backend does not emit "ride:free_window_ended". The actual event is "ride:waiting:charge:started" (WAITING_CHARGE_STARTED). Audit invented a non-existent event name.
driver:checkin_required — no listener	FRONTEND BUG	Backend emits "driver:checkin:required" (colons, not underscores). App has no handler.
driver:checkin_rejected — no listener	FRONTEND BUG	Backend emits "driver:checkin:rejected". App has no handler.
driver:location_updated — no listener	CONTRACT GAP	Backend does not emit "driver:location_updated". The real event is "driver:location:ack" (DRIVER_LOCATION_ACK) — a server acknowledgement. Audit invented a wrong event name.
surge:update — no listener	SHARED	Backend emits "surge:updated" (not "surge:update" — different name). App has no listener. Both the wrong name in the audit and the missing listener in the app are issues.
service:control_changed / service:settings_changed — no listener	CONTRACT GAP	Backend events are "service:control:changed" and "service:settings:changed" (three-part names with colons). Audit used wrong event names; app has no listeners either way — real bug is missing listener.
sos:triggered — no listener	FRONTEND BUG	Backend emits "sos:triggered" which matches. App has no handler.
7 socket constants defined but never emitted	FRONTEND BUG	Dead code. The constants exist in constants/socketEvents.ts but are never called — app uses REST for location/status instead. Not broken but clutters the codebase.
Mock Data & Missing UI Wiring
Audit Finding	Verdict	Reason
driver mock (avatar/name) rendered in home screen	FRONTEND BUG	GET /api/driver/me exists and is correct. Screen simply uses mock instead.
ratingsBreakdown + reviews mock in ratings screen	FRONTEND BUG	GET /api/driver/me/ratings exists. Screen never calls it.
boardingPassengers mock in boarding screen	FRONTEND BUG	Booking list is derivable from GET /api/driver/trips/:id. Screen uses mock.
Promo cards mock in earnings screen	BACKEND MISSING	No /api/driver/promotions route found in any file explored.
In-app chat button has no wiring	FRONTEND BUG	routes/chat.ts exists in the backend. App UI button is a stub.
SOS button has no wiring	FRONTEND BUG	Backend has a rides SOS endpoint. App button is a stub.
Background location locationInterval dead code	FRONTEND BUG	The ref is declared but never assigned. REST location update works; background task was never implemented.
Phone call button has no wiring	FRONTEND BUG	This is purely a frontend native call (Linking.openURL('tel:...')). No backend involved.
Waiting charge display missing	FRONTEND BUG	Backend emits ride:waiting:charge:updated; app has no UI ticker or socket listener.
Offer expiry countdown missing	FRONTEND BUG	Backend emits ride:offer_expired; app overlay never dismisses.
Summary Count
Verdict	Count
CONTRACT GAP (contract doc wrong, backend fine, app often fine too)	23
FRONTEND BUG (backend correct, app wrong or missing)	24
BACKEND MISSING (endpoint genuinely doesn't exist)	4 (shuttle/lines/activate, shuttle/lines/complete, shuttle/lines/book, driver/promotions)
SHARED (both sides contribute)	3 (register payload, todayEarnings, surge:updated name)
The biggest takeaway: the BACKEND_API_CONTRACT.md document is significantly outdated. Roughly half of the "critical" P0 findings are not real bugs — they are contract doc drift. The actual backend is substantially more aligned with the app than the audit suggests.