You are working on the VeeGo Driver App.

Important:
You have NO previous chat history.
Do not assume any previous context.
Read the existing code before making changes.

This task is ONLY small UI/UX fixes.
Do not refactor architecture.
Do not change backend.
Do not change APIs.
Do not modify trip logic, dispatch logic, navigation logic, or socket logic.

================================================

GOAL:
Apply four small UI improvements in the Driver App.

Scope:
Home screen map and ride request UI only.

First inspect:

- app/home.tsx (or the current driver home screen)
- map components used by the home screen
- theme/context files
- ride request card component
- trip details/navigation button components

================================================

CHANGE 1:
Driver Home Map Theme

Current issue:
The map theme (dark/light) follows the phone system theme.

Required behavior:
The map must follow the VeeGo app theme mode, NOT the device theme.

Example:

If VeeGo app theme = Dark:
→ map uses dark map style

If VeeGo app theme = Light:
→ map uses light map style

Requirements:
- Use the existing app theme provider/context.
- Do not create a second theme system.
- Do not change the rest of the application theme behavior.
- Only connect the map style to the existing app theme state.


================================================

CHANGE 2:
Map Controls Position

On the Driver Home screen:

Move the map mode switch button:

Current:
Wherever it is currently placed.

Required:
Place it at:
- bottom right corner of the map

Directly below it add:

Navigation/recenter button.

Order:

[ Map Theme Button ]

[ Recenter / Navigation Button ]


Behavior of recenter button:
- Return the camera to the driver's current location.
- Keep existing camera logic.
- Do not change navigation behavior.


UI requirements:
- Use existing button styles if available.
- Do not redesign the whole map UI.


================================================

CHANGE 3:
Ride Request Card Position

Current issue:
The ride request card that appears when a passenger requests a ride is positioned too low.

Required:
Move the request card slightly upward.

Only adjust:
- vertical positioning
- spacing/margin/padding if needed

Do NOT change:
- request acceptance logic
- timer
- buttons
- API calls
- socket events


================================================

CHANGE 4:
External Google Maps Navigation Button

Current issue:
The button that opens Google Maps navigation is currently inside the trip details card.

Required:

Move this action into a standalone floating button.

Requirements:

- The Google Maps navigation button must be a separate icon/button.
- It should not be inside the trip details card.
- It should appear independently on the map/trip screen.
- Keep the same functionality:
  tapping it opens Google Maps navigation.

Do NOT:
- replace navigation implementation
- add new navigation APIs
- change routing logic


================================================

Safety requirements:

Do not modify:
- Driver onboarding
- Dispatch service
- Booking flow
- Earnings
- Wallet
- Background location
- Socket events
- Backend

Only make the four UI changes above.

Before editing:
Inspect the current implementation and identify the exact files that will be changed.

After implementation provide:

1. Files changed.
2. Exact UI changes made.
3. Confirmation that business logic was untouched.
4. TypeScript/build result.