Continue the ActiveSession integration hardening work for the VeeGo Driver App.

Important context:
A previous coding session was already working on these fixes, but the token limit ended while it was implementing them.

You do NOT have the previous conversation history.
Therefore:
- First inspect the current codebase.
- Check git diff / modified files.
- Determine which changes from this task were already applied.
- Do not blindly recreate changes that already exist.
- Continue from the current state.

Project goal:
Prepare the Driver App for Shuttle pilot by making ActiveSession integration reliable.

Important architecture rules:
- ActiveSession is the recovery/session layer.
- ShuttleContext remains in place.
- Do NOT migrate the whole app to ActiveSession.
- Do NOT remove ShuttleContext.
- Do NOT change backend contracts.
- Do NOT rewrite working flows.
- Make minimal targeted fixes only.
- Preserve all existing mutations, socket flows, and navigation behavior.

The previous audit found these 5 issues that need to be fixed:

==================================================
FIX 1 — B1: Remove duplicate shuttle:referral:incoming listener
Severity: High
==================================================

Problem:
The same socket event is currently handled twice:

1) ReferralSocketBridge in:
app/_layout.tsx

2) useShuttleSocket.ts inside the shuttle route group

Both call addIncomingReferral.

Result:
- duplicate referral queue entries
- duplicate badge counts
- duplicate processing while inside shuttle screens

Required action:
- Keep only ONE listener for shuttle:referral:incoming.
- Remove the duplicate listener.
- Prefer keeping the shuttle-specific handling in the existing shuttle socket layer if that matches the current architecture.

Verify:
- grep the project and confirm only one handler remains.
- addIncomingReferral is not called twice for the same socket event.


==================================================
FIX 2 — R2: Make ShuttleProvider available during ActiveSession cold start recovery
Severity: Medium
==================================================

Problem:
When the driver closes and reopens the app during an active shuttle trip:

Flow:
1. ActiveSession initializes.
2. It detects activeSession.sessionType === "shuttle_trip".
3. It redirects directly to:
   /shuttle/trip-active

Issue:
The ShuttleProvider from:
(shuttle)/_layout.tsx

may not exist in this navigation path.

Result:
trip-active.tsx can receive:
- shuttleSession ✅
but ShuttleContext values may be empty:
- activeLine
- stops
- passengers
- stationCoords
- nextStop

GPS was already protected, but operational trip data can still be missing.

Required action:
Fix provider scope so ShuttleProvider is available when recovering directly into trip-active.

Preferred solution:
Move ShuttleProvider to a parent layout where it exists during recovery.

Requirements:
- Keep useShuttle API unchanged.
- Do not remove ShuttleContext.
- Do not break normal shuttle navigation.
- Do not duplicate providers unnecessarily.

Verify:
Cold start during active shuttle trip restores:
- stops
- passengers
- station data
- trip controls
- existing shuttle actions


==================================================
FIX 3 — R1: Add ride socket fallback for phase updates
Severity: Medium
==================================================

Problem:
File:
app/ride/[rideId].tsx

Current behavior:
These socket events are registered:
- ride:status:changed
- ride:driver_assigned
- ride:driver_arrived
- ride:started

but handlers are empty.

Current phase updates depend only on:
session:snapshot

Risk:
If snapshot delivery is delayed, ride UI phase does not update.

Required action:
Implement fallback phase updates from socket events.

Mapping:

ride:driver_assigned
→ phase = "to_pickup"

ride:driver_arrived
→ phase = "arrived"

ride:started
→ phase = "in_trip"

ride:status:changed
→ use existing status mapping if available.

Important:
- session:snapshot remains the main source of truth.
- This is only a fallback.
- Do not duplicate business logic.
- Do not break existing termination handling.


==================================================
FIX 4 — B2: Protect shuttle home redirect
Severity: Medium
==================================================

Problem:
File:
(shuttle)/home.tsx

Current redirect:

activeSessionInitialized &&
activeSession?.sessionType === "shuttle_trip"

causes:

router.replace('/shuttle/trip-active')

Risk:
If activeSession changes or another snapshot arrives, redirect may fire again unexpectedly.

Required action:
Add a guard.

Example:
hasRedirectedRef

Requirements:
- Redirect only once per mount/recovery.
- No navigation loops.
- Keep normal active trip recovery behavior.


==================================================
FIX 5 — R3: Add missing referral accepted/declined handling
Severity: Low
==================================================

Problem:

SOCKET_EVENTS contains:

REFERRAL_ACCEPTED
REFERRAL_DECLINED

but there are no listeners.

Result:
Driver does not receive updates when referral requests are accepted or declined.

Required action:
Add listeners in the correct existing socket layer.

Rules:
- Do not create a new referral system.
- Use existing referral state/notification mechanisms.
- Keep event handling consistent with current architecture.


==================================================
Verification after implementation
==================================================

Before finishing:

1. Run TypeScript check:
tsc --noEmit

2. Report:
- files modified
- which fixes were already present before this session
- which fixes were completed now
- confirmation that:
  - ActiveSession was not migrated completely
  - ShuttleContext was not removed
  - backend contracts were not changed
  - existing mutations still work

3. If any previous partial implementation exists:
- keep it if correct
- fix only incomplete or incorrect parts.