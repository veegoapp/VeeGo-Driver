We are continuing the ActiveSession migration.

Current status:
- Shared ActiveSession infrastructure is complete.
- Ride migration is complete.
- Shuttle Phase 1 and Phase 2 are complete.
- app/shuttle/trip-active.tsx already migrated:
  - tripId
  - direction
  - station count
  - progress dots source

This is SHUTTLE PHASE 3.

Use the attached ActiveSession contract as the ONLY source of truth.

IMPORTANT:
This phase focuses on derived trip progress state.
Do NOT guess mappings.
Do NOT invent fields.
Do NOT change business behavior.

Scope:

You may modify ONLY:
- app/shuttle/trip-active.tsx

Do NOT modify:
- ShuttleContext
- ActiveSessionProvider
- ActiveSessionContext
- SocketProvider
- Ride files
- Backend
- Navigation
- Shared hooks

Goal:

Audit whether current stop/progress information currently coming from ShuttleContext can safely use DriverShuttleSession.

Before changing anything:

Audit these items:

1. currentStop
2. currentStopIndex
3. isLastStop
4. station progress state
5. any UI that depends on stop progression

Compare them against:
DriverShuttleSession.stations[]
and any related fields in the attached contract.

Implementation rules:

1. If the contract provides the exact required information:
   - migrate it to DriverShuttleSession.

2. If migration requires:
   - recreating ShuttleContext calculations,
   - assuming array ordering,
   - changing business logic,
   - changing mutation behavior,
   - changing server progress interpretation,

   DO NOT migrate it.

3. Keep ShuttleContext fallback where migration is unsafe.

4. Do not modify:
   - boarding logic
   - no-show logic
   - finish route logic
   - location tracking
   - ETA calculations
   - socket handlers

5. Do not remove old code.
6. Do not cleanup unused code yet.

After implementation:

Run:

tsc --noEmit

Return:

- Modified files.
- Whether currentStop/currentStopIndex were migrated or not.
- Every migrated field.
- Every field still using ShuttleContext and exact reason.
- Confirmation Ride files were untouched.
- Confirmation ActiveSession infrastructure was untouched.
- TypeScript result.