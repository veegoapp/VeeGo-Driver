The attached ActiveSession contract remains the ONLY source of truth.

We are implementing ONLY the shared foundation.

Do NOT modify:
- Ride interface
- Shuttle interface
- Any screen
- Any navigation
- Any recovery flow
- Any existing socket behavior
- Any API endpoint
- Any business logic

This task is ONLY to activate the existing REST initialization flow.

Implementation requirements:

1. In app/_layout.tsx add a new render-null component named:

   ActiveSessionInitBridge

2. The bridge must use:
   - useAuth()
   - useActiveSession()

3. The bridge must call initializeActiveSession() ONLY when ALL of the following are true:

   - auth loading has completely finished
   - a valid token exists
   - initialization has not already been executed for the current login session

4. Use a useRef guard so initializeActiveSession() executes exactly once per authenticated session.

5. When the token becomes null (logout), reset the guard so the next successful login performs a fresh initialization.

6. Do NOT use navigation.

7. Do NOT read session.

8. Do NOT read initialized.

9. Do NOT trigger refreshActiveSession().

10. Do NOT modify ActiveSessionProvider.

11. Do NOT modify postAuthRouter.ts.

12. Do NOT introduce callbacks, bridges, emitters, module globals, or pub/sub mechanisms.

13. Do NOT change SocketProvider.

14. Do NOT remove or modify the existing session:snapshot listener. It must continue working exactly as it does today.

15. Mount <ActiveSessionInitBridge /> alongside the existing root bridge components inside app/_layout.tsx.

Verification required after implementation:

- List every modified file.
- Explain exactly when initializeActiveSession() now executes.
- Confirm it executes exactly once per authenticated session.
- Confirm logout resets the guard.
- Confirm no Ride logic changed.
- Confirm no Shuttle logic changed.
- Confirm no navigation changed.
- Confirm no recovery flow was removed.
- Run TypeScript and report the result.

Zero functional changes outside this shared infrastructure are allowed.