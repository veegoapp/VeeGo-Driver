Task: Implement Phase 2 of the VeeGo Driver App Navigation Improvements.

IMPORTANT CONTEXT:
This is a continuation of a previous completed phase, but this chat session has NO previous history.

Phase 1 has already been completed successfully.

Current Phase 1 status:

Files changed:
- app/ride/[rideId].tsx only

Implemented:
1. Ride / Scooter / Delivery now use the existing useRoadPolyline hook.
2. The generated roadPolyline is passed to MapBackdrop through the existing roadPolyline prop.
3. navigationMode is now enabled during:
   - to_pickup
   - in_trip
4. Shuttle was not modified.
5. MapBackdrop.native.tsx was not modified.

Current architecture:
- MapBackdrop.native.tsx controls:
  - map rendering
  - polyline rendering
  - camera behavior
  - follow mode
  - recenter behavior

- useDriverLocation.ts controls:
  - GPS updates
  - heading
  - speed
  - driver position

Do NOT undo or redesign Phase 1 changes.

==================================================

PHASE 2 GOAL:
Improve the existing navigation experience to feel closer to Uber/Careem driver navigation.

IMPORTANT:
- Do NOT change routing architecture.
- Do NOT remove useRoadPolyline.
- Do NOT remove existing navigationMode behavior.
- Do NOT add Google Navigation SDK.
- Do NOT add new paid APIs.
- Do NOT implement voice navigation.
- Do NOT implement rerouting yet.
- Do NOT implement route trimming yet.
- Do NOT make unrelated refactors.

==================================================

Implement the following improvements:

1. Improve GPS update frequency

Inspect:
hooks/useDriverLocation.ts

Current behavior:
- Around 3000 ms update interval
- Around 10 meter distance interval

Evaluate and improve for navigation use:

Target:
- timeInterval around 1000 ms
- distanceInterval around 3-5 meters

Consider:
- Battery impact
- Existing background tracking behavior
- Do not break shuttle tracking or driver online status.

==================================================

2. Improve camera animation smoothness

Inspect:
components/MapBackdrop.native.tsx

Current navigation camera already exists:
- animateCamera()
- pitch
- bearing
- zoom
- follow mode

Improve only the smoothness:

Check:
- animDurationMs
- animation timing versus GPS update frequency
- possible camera jumps

Goal:
- Smooth movement while driving.
- Avoid camera lagging behind location updates.

==================================================

3. Add bearing smoothing

Current behavior:
- Heading comes from GPS.
- calcBearing() is used as fallback.

Problem:
Raw GPS heading can cause sudden rotation jumps.

Implement a lightweight smoothing approach.

Example:
- EMA / low-pass style smoothing.

Requirements:
- Keep existing heading logic.
- Do not break shuttle arrow rotation.
- Do not create a complex new system.

==================================================

4. Improve driver marker movement

Inspect how the driver marker updates.

Goal:
Instead of appearing to jump between GPS points:

Current:
Location A → wait → Location B

Improve toward:
Location A → smooth movement → Location B

Keep compatibility with:
- Ride
- Scooter
- Delivery
- Shuttle

==================================================

5. Verify current navigation camera behavior

After changes, verify:

Ride:
- to_pickup navigation mode
- in_trip navigation mode

Shuttle:
- navigationMode=true remains working

Home:
- idle map behavior unchanged

==================================================

Before modifying code:

Inspect the current implementation and confirm:
1. Which files need changes.
2. Any risks.
3. Whether each requested improvement already partially exists.

After implementation provide:

1. Files changed.
2. Exact changes.
3. TypeScript/build verification.
4. Confirmation that:
   - Phase 1 behavior remains intact.
   - Shuttle was not broken.
   - No new APIs were added.
5. Any remaining navigation limitations.

Do not continue to Phase 3.