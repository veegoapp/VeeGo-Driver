فى اخر سيشن كنت طلبت منك البرومت ده 
TASK INSTRUCTIONS: This is a PLANNING AND ANALYSIS ONLY request. DO NOT modify, write, or execute any code yet. 
Review the codebase against the requirements below and present a detailed implementation plan + answer the architectural question.

---
### SCOPE OF REQUESTED CHANGES (Profile, Support, & Messages):

1. **Profile Screen Avatar (`app/(tabs)/profile.tsx`):**
   - Update the top profile avatar component to dynamically fetch and display the actual Driver Profile Photo uploaded during sign-up/onboarding instead of a placeholder.

2. **Personal Information Screen (`app/profile/personal-info.tsx`):**
   - Remove the "Locked" badge/labels currently displayed on driver data fields. Keep all fields Read-Only, but clean without the explicit "Locked" text.
   - Ensure the Driver Name field is also set to Read-Only.
   - Completely remove the "License Number" and "National ID" fields from this screen.

3. **Bonus Targets Icon Bug:**
   - Diagnose and fix why the icon for the "Bonus Targets" item on the main Profile screen is currently invisible/broken.

4. **Help & Support Screen Restructure:**
   - Completely remove the "Chat with Us" and "Call Support" action options.
   - Refactor "Browse Topics" to properly display a curated list of common driver FAQs/support topics.
   - Refactor "Submit Ticket" into a structured Card:
     - Tapping it must first prompt the driver to select the specific Trip related to the issue.
     - Upon selecting a trip, display predefined issue categories/types.
     - Allow entering a description and uploading attachment photos before submitting.

5. **Messages Screen (`app/profile/messages.tsx` or equivalent):**
   - Implement a 2-Tab layout:
     - Tab 1: **Notifications** (System/App alerts).
     - Tab 2: **Support Messages** (Responses/updates on submitted support tickets).

---
### QUESTION & DELIVERABLES REQUIRED FROM YOU:

1. **Question:** Explain the difference between `app/(tabs)/trips.tsx` and the `Ride History` route under Profile in the current codebase (e.g., are they redundant, or do they serve different roles?).
2. **Implementation Plan:** Provide a clean, step-by-step file-by-file plan for implementing all 5 requirements above.

WAIT FOR MY EXPLICIT APPROVAL BEFORE WRITING OR CHANGING ANY CODE.
 و انته رديت عليا بالرد ده 

I have all the context needed. Here is the full analysis and plan.

Architectural Question: app/(tabs)/trips.tsx vs Ride History under Profile
They are not redundant — they cover entirely different service types and serve different purposes:

app/(tabs)/trips.tsx
app/ride/history.tsx (Profile → Ride History)
Service type
Shuttle (fixed-route)
On-demand (car/scooter/delivery)
Data type
DriverTrip
RideHistoryItem
API endpoint
endpoints.trips.list(status, page)
endpoints.rides.history(page, limit, status)
Purpose
Active management — accept, reject, start, complete, cancel assigned shuttle routes
Read-only log — completed/cancelled ride earnings and passenger info
Access point
Bottom tab bar
Profile menu → "Ride History"
Bottom line: trips.tsx is an operational control panel for the shuttle service. ride/history.tsx is a passive historical record for on-demand rides. Neither can replace the other.

Implementation Plan
Item 1 — Profile Screen Avatar (app/(tabs)/profile.tsx)
Root cause to confirm first: The current code already chains driver.avatar → profile_photo document → ui-avatars placeholder. The likely issue is that driver.avatar is null from the backend and the profile_photo document type key doesn't match what the onboarding upload stores (it may be stored as 'selfie', 'driver_photo', or similar). Need to verify the exact document type used during onboarding.
Plan:
Read app/(onboarding)/ screens that upload the profile photo to confirm the exact document type string sent to the backend.
In app/(tabs)/profile.tsx, update the documentsArray.find() fallback chain to use the confirmed type key(s) — add a second .find() with alternate type names if needed.
If driver.avatar returns a relative path (not a full URL), prefix it with the API base URL from EXPO_PUBLIC_API_URL.
Keep the ui-avatars.com final fallback as-is.
Files touched: app/(tabs)/profile.tsx (minor edit, 1–3 lines)

Item 2 — Personal Information Screen (app/personal-info.tsx)
Three sub-changes, all in the same file:
A. Remove "Locked" badges
Delete the {field.editable === false && editing && (...lockedBadge...)} conditional block (lines 195–199).
Remove lockedBadge and lockedText from StyleSheet.create.
B. Make Name field read-only
Add editable: false to the name field in the FIELDS array.
Since no field is now editable, remove the Edit/Save button from the header (the <Pressable> with Edit3/Check icons, lines 122–135).
Remove editing state, setEditing, the handleSave function, and updateMutation — they become dead code.
Simplify the useEffect to just initialize form from raw (keep form state only if the gender display logic needs it, otherwise replace with direct raw?.fieldName reads).
Remove useMutation import if no longer used.
Remove Edit3, Check from lucide imports.
C. Remove License Number and National ID fields
Delete the two entries for licenseNumber and nationalId from the FIELDS array (lines 101–102).
Remove licenseNumber and nationalId from the form state initialization in useEffect.
Remove CreditCard and FileText from lucide imports (now unused).
Remove licenseNumber and nationalId from the DriverMe local type if kept (or keep the type as-is since it's harmless).
Files touched: app/personal-info.tsx (moderate refactor, ~30 lines deleted)

Item 3 — Bonus Targets Icon Bug (lib/iconMap.tsx)
Root cause (confirmed): app/(tabs)/profile.tsx line 202 passes icon="target" to MenuItem, which calls FeatherIcon({ name: "target", ... }). In lib/iconMap.tsx, ICON_MAP has no 'target' key, so FeatherIcon hits line 81 (if (!Icon) return null) and renders nothing.
Fix:
In lib/iconMap.tsx, add Target to the lucide import line.
Add 'target': Target to ICON_MAP.
Files touched: lib/iconMap.tsx (2-line edit)

Item 4 — Help & Support Screen Restructure (app/support.tsx)
This is a significant structural rewrite. The current screen conflates topic selection with ticket submission. The new design separates them clearly into two independent sections.
Current → New structure:
Section
Current
New
Contact cards
Chat + Call
Removed entirely
Browse Topics
Clickable rows that also select the topic for submission
Read-only FAQ accordion (tap to expand a hardcoded answer)
Submit Ticket
Inline form immediately below topic selection
Dedicated multi-step card
Browse Topics — new FAQ accordion:
Keep the same 6 TOPIC_DEFS keys but add a faqAnswer string (translated) to each entry.
Render each topic as an expandable row: tap to toggle, shows the answer text below the label.
No selection state; purely informational.
Submit Ticket — new 3-step flow: The multi-step flow lives in the same screen as an expandable/modal card, progressing through steps:
Step 0 (collapsed): A "Submit a Ticket" button/card. Tapping opens Step 1.
Step 1 — Select Trip: Fetch recent trips via endpoints.rides.history(1, 20, undefined) (on-demand) and endpoints.trips.list(undefined, 1) (shuttle, optional). Display as a scrollable list of trip rows (date, pickup → dropoff or route name). Driver must select one. "My issue is not about a specific trip" option included (maps to null tripId). Confirm → Step 2.
Step 2 — Select Issue Category: Show the 6 existing SupportCategory buckets as selectable chips/cards (Payment, Safety, Quality, Refund, Lost & Found, Other). Back button returns to Step 1. Select one → Step 3.
Step 3 — Describe & Attach: Existing description TextInput + attachment photo picker (keep existing handleAddPhoto, removeAttachment, upload logic). Submit button calls endpoints.support.submitTicket with the selected category, the trip ID (if selected), and the description. Back button returns to Step 2.
Success state: Existing "submitted" UI (keep as-is).
State changes:
Remove: selectedTopicKey state (replaced by step-specific state)
Add: ticketStep (0 | 1 | 2 | 3), selectedTripId (string | null), selectedCategory (SupportCategory | null)
Keep: description, attachments, isSubmitting, submitted, failedAttachmentCount
Imports to remove: MessageCircle, Phone, Search, ChevronRight (from lucide), Linking, getToken, getUserIdFromToken Imports to add: useQuery (from react-query, for fetching trips), FlatList or additional ScrollView usage for trip list
Files touched: app/support.tsx (major rewrite) New translations needed (both en.ts and ar.ts): FAQ answer strings for each topic, step labels (select_trip, select_category, no_specific_trip, step progress indicators)

Item 5 — Messages Screen 2-Tab Layout (app/messages.tsx)
Current: Single list of Notification[] from endpoints.notifications.list().
New: Two tabs side by side — Notifications | Support Messages.
Tab 1 — Notifications: Exact same rendering as the current screen (title, body, unread dot, mark-read on tap, "Mark all read" button). No changes to data or logic.
Tab 2 — Support Messages: Displays submitted ticket threads/responses. Requires checking whether endpoints.support exposes a getTickets() or listTicketMessages() endpoint. If the endpoint exists, render each ticket as a card showing: subject, status badge (open/resolved), last update time, and the most recent staff reply. If no such endpoint exists yet, render an empty state ("No support responses yet") with a placeholder shape — the tab shell is built now, data wired later.
Implementation approach:
Add [activeTab, setActiveTab] = useState<'notifications' | 'support'>('notifications') state.
Render a tab bar row (two Pressable chips, same style as the earnings period chips) below the page title.
Conditionally render Tab 1 or Tab 2 content.
Keep all existing notification logic (query, mark-read mutation, mark-all-read) as-is — just conditionally shown.
Files touched: app/messages.tsx (moderate — add tab state/UI, add support query, ~50 lines added)

File-by-File Summary
File
Change size
Nature
lib/iconMap.tsx
Tiny (2 lines)
Bug fix
app/personal-info.tsx
Small (~30 lines deleted)
Cleanup
app/(tabs)/profile.tsx
Tiny (1–3 lines)
Bug fix / data wiring
app/messages.tsx
Medium (~50 lines added)
Feature addition
app/support.tsx
Large (major rewrite)
Full restructure
lib/i18n/translations/en.ts + ar.ts
Medium
New translation keys for FAQs + ticket steps
Sequencing recommendation: Items 3, 2, and 1 are small and independent — do them first in parallel. Item 5 (Messages) is self-contained. Item 4 (Support) is the largest and should go last since it needs new translation keys and a new multi-step state machine.

Waiting for your approval before writing any code.

فا انا رديت عليك بالرد ده 
Plan approved! You may proceed with implementing all the requested changes (Profile avatar fix, Personal Info cleanup, Bonus Targets icon fix, Messages 2-Tab layout, and Help & Support restructure).

CRITICAL INSTRUCTION: Do NOT touch or modify `app/(tabs)/trips.tsx` or `app/ride/history.tsx` at all right now. We will handle those in a separate dedicated prompt later.

Ensure zero TypeScript errors upon completion and keep changes minimal and clean.
 و انته بدات تشتغل و عملت كده 
I'll work on all 5 items in parallel. Let me kick off the reads I still need while applying the two already-clear fixes simultaneously.
Good. I need to check the documents endpoint shape to confirm the avatar fix before writing it. Let me read that alongside starting the big parallel edits.
Good. The avatar bug is ?? vs || — if driver.avatar is an empty string, ?? doesn't fall through to the documents photo. I also need to read the rides history type and the i18n file ends before writing the new translations.
I have everything I need. Let me now check RideHistoryItem in the types file and then implement all 5 items in one big parallel batch.
t.back exists. I have everything I need — now writing all 6 files in one parallel batch.
You've reached your daily free quota limit. It will reset at 12:00 AM UTC.

و زى ما انته شايف التوكنز خلصت قبل ما تخلص فا شوف انته وصلت لفين و كمل من مكان ما وقفت 